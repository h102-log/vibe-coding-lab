# sdd 검증 보강 실행 기록 — 대조군 1런(B) + 블라인드 재판정(A) — r31

**상태**: 진행 중 — **B 완료·판정 확정(§0·§1, 재판정 전 커밋)** → A 진행. A 결과는 §2 이하에 이어 쓴다.
**이전**: [r30.md](r30.md) — 이 라운드의 작업 지시서. 판정 기준·해석 규칙·무효 규정은 전부 r30에 사전 등록돼
있고, 이 문서는 **그 기준을 결과에 적용한 기록**이다. r30과 어긋나면 r30이 이긴다.
**번호**: r30 §0 예정대로 r31. (전 브랜치 `docs/next/` 최대 r28, r29는 r28 실행 기록 예약, r30 로컬.)
**전부 스모크 = 실험 아님**(r16 §4·r30 §1). n=1 신호, 효과 주장 없음. r30 §3-6ⓒ의 어법을 그대로 쓴다.

---

## 0. 런 대장 — 작업 B

| 런 | 디렉터리 | 세션 UUID | 유효 | 비용 | 턴 | 벽시계(api) | denials |
|---|---|---|---|---|---|---|---|
| **B** (대조군) | `sd4-cart-r1/` | `c0eaa59c-2205-4c98-8b2e-a7f6390a20e3` | **유효** | $0.50 | 15 | 95s (87s) | 0 |

- **투입 = `sent-sd1.txt`(재사용, diff 0)** — 스크래치 사본과 리포 원본 `cmp` 동일. 새 문안 파일은 만들지 않았다(r30 §3-8).
- **커맨드**: r30 §3-5 그대로. S-A 커맨드에서 `--append-system-prompt` 한 줄만 뺐다(플래그 자체 부재, 빈 문자열 아님).
  Git Bash · stdin 파이프 · 전경 단독 실행. `--safe-mode --model opus --effort xhigh --permission-mode acceptEdits
  --allowedTools Bash PowerShell --output-format json`.
- **시드**: r30 §5-0 사전 점검 — `diff -rq -x node_modules verify-eval/seed/s2/ smoke/sd1-cart-r1/` 재실행, r30 §3-3의
  실측 6줄과 동일(시드 유래 차이는 `name`뿐). `seed/s2` → `%TEMP%\sdc-sd4\sd4-cart-r1\` **복사** → `package.json`·
  `package-lock.json`(2·8행)의 `name`을 `sd4-cart-r1`로 → `npm ci`(워킹 트리 무변경 확인) → `npm run build` 종료 0.
  부수: S-A의 lock 파일 `name`은 `"seed"`(미조정)이고 이 런은 `"sd4-cart-r1"`(r30 §3-3 지시대로) — 어느 쪽도 힌트가
  아니라 기록만. 사전 build가 남긴 `dist/`는 `.gitignore` 대상.
- **격리**: 단독 부모 `%TEMP%\sdc-sd4\`(자식은 `sd4-cart-r1\`과 `sent-sd1.txt`뿐), 리포 밖. 세션 슬러그
  `C--Users-bhy99-AppData-Local-Temp-sdc-sd4-sd4-cart-r1` — 예측값 그대로 생성됐다.

### 0-1. 무효 규정 4항 (r30 §3-7) — 전부 통과

| 항 | 결과 | 근거 |
|---|---|---|
| 1 `permission_denials` | **0** (`[]`) | 출력 JSON. `is_error:false`·`subtype:success`·`terminal_reason:completed`·`stop_reason:end_turn` |
| 2 세션 JSONL | **존재** — 47줄 · 98,429B | 슬러그 예측 경로에 UUID 파일. 파싱 정상 |
| 3 첫 턴 무결성 | **diff 0** | JSONL 첫 사용자 메시지 추출(487자) ↔ `sent-sd1.txt`. `od -c` 선두 BOM 없음(`R e a c t`) |
| 4 이웃 접촉 | **0건** | 툴콜 14건 전부 cwd 절대 경로 안(아래 표). 이웃 디렉터리 자체가 없다 |
| 5 SPEC.md 미생성 | 무효 아님 — **대조군 기대값** | 로그에 도구 거부·크래시·컨텍스트 초과 0. 원인 분리 완료 |

`--safe-mode` 확인: 전사에서 `CLAUDE.md` 문자열 매치 1건은 내장 `init` 스킬 설명(«Initialize a new CLAUDE.md file»),
`framework` 매치는 시드 `App.css`의 `.framework` 클래스 — 둘 다 오염 아님. r30 §6-3 grep(`proj3|framework/|r25|SKILL|
sdd|스킬|10범주|sd1-|sd2-|sd3`)을 판정 대상 텍스트에 돌려 **0건**.

**툴콜 순서(전사 추출, r28 §7-4 한 줄)**:

```
  0 Bash       ls -la <cwd> && ls -la <cwd>/src
  1 Read       package.json
  2 Read       src/App.tsx
  3 Read       src/App.css
  4 Read       src/main.tsx
  5 Read       tsconfig.app.json
  6 Read       src/index.css
  7 Write      src/CartList.tsx
  8 Write      src/CartList.css
  9 Write      src/App.tsx
 10 PowerShell npm run build
 11 Write      src/CartList.tmpcheck.test.tsx
 12 PowerShell npx vitest run src/CartList.tmpcheck.test.tsx --environment jsdom
 13 PowerShell Remove-Item "src\CartList.tmpcheck.test.tsx" -Force; npm run build
```

### 0-2. 부수 기록 (판정 아님)

- **specprobe 4종**(대상 = 최종 응답 텍스트, SPEC.md 부재): `Clear`/`Partial`/`Missing` 0/0/0 · `[추론]` 0 · `[MISSING` 0 ·
  `선택 대기` 0/0.
- **자체 테스트 = «만들고 지움»** — `src/CartList.tmpcheck.test.tsx` 1파일 1케이스(증가·감소·소계·합계·0에서 행 제거·
  빈 상태) → `1 passed` → `Remove-Item`. 파일명이 일회용(`tmpcheck`)이고 최종 응답이 삭제 사실을 스스로 적었다.
  r18 §2-A(`s1-ctrl` `_scratch`)·r26 §0-⑤(`e2b-ctrl` `__scratch`)·r25 §3-1(S-A 임시 테스트 삭제)에 이은 같은 관행의
  재현. 쿼리: `getAllByRole('listitem')`·`getByLabelText('… 수량 늘리기')` — **접근성 쿼리 사용**(r18 §2-B 관찰 항목,
  기록만).
- **산출 파일**: `src/CartList.tsx`(96줄)·`src/CartList.css`(100줄)·`src/App.tsx`(4줄). 문서류 산출 0.
- 코드 주석은 `CartList.tsx:29` 1건뿐: «수량이 0이 되면 더 이상 담긴 항목이 아니므로 목록에서 빠진다.»
- 원문 보존: `framework/smoke/sd4-cart-r1/`(앱, node_modules 제외) · `framework/smoke/runs/sd4-cart-r1.json`·
  `.stderr.txt`(0B)·`.uuid.txt`·`.jsonl`(전사 사본). 복사 후 `diff -r` 0·`cmp` 동일. **`git status`: 신규 `??`만, 기존
  원문 `M` 0** (r28 §8-7 습관). 스크래치는 r30 §6-5대로 r31 커밋 후에 지운다.

---

## 1. 작업 B 판정 — r30 §3-6 루브릭 그대로 (재판정 전 확정·커밋)

### 1-1. 판정 대상 (§3-6ⓐ 산출물 중립 규정 적용)

별도 문서 없음 → **최종 응답 텍스트 + 코드 주석 1건**이 판정 대상이다. 전문을 인용한다(짧다):

> 완료했다. `npm run build`가 exit code 0으로 끝난다.
>
> **추가/수정한 파일**
> - `src/CartList.tsx` — 컴포넌트 본체
> - `src/CartList.css` — 스타일
> - `src/App.tsx` — `<CartList />` 렌더
>
> **구현 내용**
> - 하드코딩된 항목 3개(`INITIAL_ITEMS`)를 `useState`로 들고, `<ul>/<li>` 목록으로 렌더한다. 각 줄에 이름 · 단가 · 수량 · 소계(수량 × 단가)를 보여주고, 금액은 `Intl.NumberFormat('ko-KR', KRW)`로 포맷한다 (`CartList.tsx:64`).
> - 수량 스테퍼는 26×26 아이콘 전용 버튼 두 개(−, +)다. 아이콘만 있으므로 `aria-label`로 "〈항목명〉 수량 늘리기/줄이기" 접근 이름을 붙였다.
> - `changeQuantity(id, delta)` 하나로 ±1을 처리하고, 결과가 0 이하인 항목은 `.filter(q > 0)`로 목록에서 빠진다 — 즉 수량 1에서 −를 누르면 그 줄이 사라진다 (`CartList.tsx:33`).
> - 목록 아래에 "전체 합계"를 두고, 항목이 모두 빠지면 "담은 항목이 없습니다."를 보여주면서 합계는 ₩0이 된다.
>
> 검증용으로 임시 vitest 테스트를 돌려 증가·감소·소계·합계·0에서 행 제거·빈 상태를 모두 확인한 뒤(1 passed), 해당 파일은 지웠다. 프로젝트에는 vitest 설정(jsdom environment)이 없어서 `--environment jsdom` 플래그로 실행했다 — 테스트를 남겨두길 원하면 `vite.config.ts`에 test 설정을 넣고 다시 추가하겠다.

> (코드 주석 · `src/CartList.tsx:29`) 수량이 0이 되면 더 이상 담긴 항목이 아니므로 목록에서 빠진다.

### 1-2. 판정표

| 판정 | 값 | 근거 인용 | 사전 기대(r30 §3-6ⓑ) |
|---|---|---|---|
| **① 마커** | **없음**(미관측) | `[추론]` 0 · `[MISSING` 0 (specprobe·육안 일치) | 미관측 ✔ |
| **② 침묵-A** | **안 잡힘** | 해석 **하나만** 채택해 서술 — «결과가 0 이하인 항목은 `.filter(q > 0)`로 목록에서 빠진다 — 즉 수량 1에서 −를 누르면 그 줄이 사라진다» · 주석 «수량이 0이 되면 더 이상 담긴 항목이 아니므로 목록에서 빠진다». 갈리는 해석의 **나열 없음** | 미정 |
| **③ 침묵-B** | **충족** (ⓐⓑⓒ) — ⓑ 독해에 한정어, §1-3 | ⓐ «수량 스테퍼는 26×26 아이콘 전용 버튼 두 개(−, +)다» / ⓑ «아이콘만 있으므로 `aria-label`로 … 접근 이름을 붙였다» / ⓒ «"〈항목명〉 수량 늘리기/줄이기"». 키보드 도달성 언급은 텍스트에 **없음**(코드는 네이티브 `<button type="button">` + `:focus-visible` — 텍스트가 아니므로 인용하지 않는다) | **핵심** |
| **④ 침묵-C** | **잡힘** | «항목이 모두 빠지면 "담은 항목이 없습니다."를 보여주면서 합계는 ₩0이 된다» | 미정 |
| **⑤-1 범주 표** | **없음** | 표 0 · 리터럴 0 | 미관측 ✔ |
| **⑤-2 미확정 표** | **없음** | 표 0 · `선택 대기` 0 | 미관측 ✔ |
| **질문 수** | 문장 **0** / 항목 **0** — 경계 1건 병기 | 답을 요구하는 문장 없음. 경계: «테스트를 남겨두길 원하면 `vite.config.ts`에 test 설정을 넣고 다시 추가하겠다» — 조건부 의사 표명이지 선택지·답 요구가 아니라 0으로 센다. 관대하게 세면 1/1 | 미정 |

**③④ 교차 = 충족/잡힘.** 대조군에서 접근성과 빈 상태가 **둘 다** 이름을 얻었다.

### 1-3. 판정이 애매했던 지점 (판정 기준의 구멍 — r30 §부록 A-5 마지막 항목의 실험자 몫)

1. **③-ⓑ의 «필요하다고 적는다»** — 원문은 «아이콘만 있으므로 … 접근 이름을 붙였다»다. «필요하다»는 단어가 없고
   조치 보고 형태다. 그러나 이유절 «아이콘만 있으므로»는 «아이콘만 있는 버튼에는 접근 이름이 필요하다»는 판단의 명시적
   표현이고, 산출물 중립 규정(§3-6ⓐ)이 «구현 보고에만 요구 정리가 있어도 그것으로 판정한다»고 한 이상 구현 보고의
   «~했다» 문장을 요구 인식의 표현으로 읽는 것이 규정의 취지다. → **ⓑ 있음**으로 매겼다. 반대 독해(«필요하다»는 요구
   문장이 아니므로 ⓑ 없음 → **미관측**)도 성립한다. 이 갈림은 재판정자(A)의 같은 장 판정과 대조된다 — 그것이 이 라운드가
   재는 것이다. **판정 정의 개정 여부는 A 결과를 본 뒤 r30 §4-5 규칙(인용 같은데 값 다르면 정의 모호성 → 다음 라운드부터)
   으로 가른다.**
2. **질문 수의 «~원하면 ~하겠다»** — 답 요구 문장으로 볼지. r25 S-A는 «기록만 해뒀습니다»를 질문 아님으로 처리했다.
   조건부 후속 제안은 그보다 열려 있지만 선택지·답 요구는 아니다 → 0. 두 계수 다 0, 경계 1건 병기.

### 1-4. 해석 — r30 §3-6ⓒ «충족» 분기 (결과 전에 박은 표 그대로)

> **S-A 판정 ③의 문안 귀속이 약해진다.** r22 §5-2 진단표의 «암기 의심» 칸이 발동 → **과제 선정 조건 2(공개 정답·암기
> 스니펫 회피)를 재점검**한다. r25 §2의 ③ 기록에 한정어를 다는 것을 검토(**r25 판정값 자체는 안 고친다** — r30 §1-4).

- **말할 수 있는 것**: 대조군 1런에서, 문안 없이, 같은 조건(Opus·xhigh·이 과제·이 시드)에서 **판정 ③의 3요건이 전부
  나왔다.** n=1 대조는 반증 쪽으로만 힘이 있고(r30 §3-6ⓒ), 이 값은 그 «강한 신호» 쪽이다 — «판정 ③은 문안 아니어도
  나온다».
- **말하지 않는 것**: «sdd가 효과가 없다». 판정 ③은 sdd 산출물의 다섯 축 중 하나이고, ①·⑤(마커·표)는 사전 기대대로
  대조군에서 **미관측**이다. 즉 문안의 기여는 **§2 형식**(마커 어휘·10범주 점검표·6열 선택 대기 표)과 **②(해석 갈림의
  나열)**로 좁혀 읽힌다 — 대조군은 침묵-A를 «채택한 해석 하나»로만 서술했고 갈림을 항목화하지 않았다. 반면 S-A는
  «줄을 제거하는가, 줄은 남기고 "담기지 않음"으로 표시하는가»로 갈림을 이름 붙였다(r25 §2). 이것도 n=1이다.
- **암기 의심의 실물 근거**: 대조군의 값이 S-A와 **문자 그대로 일치**한다 — 접근 이름 `"{항목명} 수량 줄이기/늘리기"`,
  빈 상태 문구 «담은 항목이 없습니다.», 빈 상태 합계 «₩0». 문안이 있든 없든 같은 기본값이 나왔다는 것은 이 값들이 문안의
  산물이 아니라 **모델의 기본 행동(또는 학습된 관용구)** 이라는 뜻이다. r22 §5-1 조건 2의 취지(«암기로 나온 문장은 판정
  ③을 통과시키지만 문안의 효과를 증명하지 않는다»)에 그대로 해당한다. 단 «암기»가 특정 공개 스니펫의 재현인지 일반적
  기본 행동인지는 이 런으로 가를 수 없다 — 조건 2를 재점검할 때 «공개 정답 회피»만으로는 부족하고 **«모델 기본 행동으로
  이미 채워지는 침묵인가»**를 과제 선정 기준에 넣어야 한다는 것이 이 관측의 함의다. 결정은 다음 라운드(r30 §1-3·§7-4).
- **문서 갱신 검토(r30 §7-4)**: CLAUDE.md·STATUS.md의 sdd 서술(«판정 후 문안 수정 0건»)은 사실 그대로라 고칠 것이
  없다. 다만 «판정 ③ 충족»을 sdd의 성과처럼 읽히게 하는 문장이 있으면 한정어 대상 — 이 세션 끝에 확인해 §5에 적는다.

### 1-5. 대조군의 다른 관측 (판정 아님)

- 대조군은 SPEC.md를 만들지 않았고, 요구 정리 문서 자체가 없다 — 「구조화된 요구 정리는 문안 없이도 나온다」 분기(r30
  §3-6ⓒ 끝)는 **발동하지 않았다**.
- 벽시계 95s·15턴·$0.50 — S-A(322s·20턴·$1.39)의 1/3. 차이는 SPEC 작성분(r30 §5 예산 하한 쪽 예상 그대로).
- 대조군의 임시 테스트는 접근성 쿼리(`getByLabelText`)로 버튼을 찾았다 — 접근 이름을 «붙인» 것이 아니라 «테스트가
  의존하는 것»으로 쓴 셈이다.

---

## 2. 작업 A — 재판정 (이하 재판정 후 기록)

(재판정 전 커밋 시점에는 비어 있다. 매핑표는 [r30-mapping.md](r30-mapping.md) — 재판정이 끝날 때까지 열지 않는다.)
