# 4단계(통합) 2축 스모크 + 실물 SPEC 재판정 — 실행 기록 (r34 집행) — r35

**상태**: 진행 중 — 작업 A·B 완료·판정 확정(이 커밋). 작업 C(실물 재판정 3세션)는 §3에 채운다.
**이전**: [r34.md](r34.md) — 작업 지시서. 판정 기준·무효 규정·한계는 전부 r34에 사전 등록돼 있고 이 문서는 **그 기준을 결과에 적용한 기록**이다. r34와 어긋나면 r34가 이긴다.
**번호**: r34가 예약한 r35. 날짜 폴더는 세션 날짜(2026-08-16).
**성격**: 스모크 = 실험이 아니다(r16 §4). n=1 신호, **효과 주장 없음**.
**한 줄**: 통합 2축 스모크 1런 유효(37턴·$2.63). 판정 **I 충족**(SPEC이 첫 Write) · **II 충족**(테스트 4파일 전부가 SPEC 문장을 원문 인용) · **III 남김**(13케이스 잔존·전건 통과 — sdd 단독의 «만들고 삭제»와 다른 값, 단 n=1 대 n=1) · **IV 오염 관측**(`선택 대기` 10행 중 4행의 기본값이 테스트 단언으로 굳었다 — r22 §4 한계 ⑤가 예고한 경로의 첫 발현).

---

## 0. 미결 확정 (r34 §6 — 사용자 답)

| # | 미결 | 확정 | 출처 |
|---|---|---|---|
| 1 | 과제 | **cart 유지** | 사용자 «전부 기본안» |
| 2 | 판정 ③④를 축에서 뺀다(조건 5 ⓐ) | **뺀다** | 〃 |
| 3 | RUBRIC v2.2 개정 후보 5건 | **안 묶는다** — v2.1 그대로 실물 1회 | 〃 |
| 4 | 요구 문안 | **`sent-sd1.txt` 재사용**(cmp 동일) | 〃 |
| 5 | 재판정 세션 수 | **3** | 〃 |
| 6 | 무효 재실행 상한 | **작업별 1회** | 〃 |
| 7 | edd의 자리 | **이 라운드에서 정하지 않음** | 〃 |
| — | 비용 체크포인트 $8 | **넘으면 턴 경계에서 멈추고 사용자 판단** (r34 기본값 — r33처럼 사전 지시로 넘기지 않는다) | 사용자 답 |
| — | 작업 A 문안 수정 | **임의 수정 금지** — 근거와 함께 보고 후 승인 시에만 | 사용자 답 |

## 1. 작업 A — 조합 연결 문안 검토 (런 없음 · 수정 0건)

### 1-1. 직접 참조 0건

`framework/skills/tdd/SKILL.md`(16줄)는 **`SPEC.md`라는 파일 이름만** 부르고 sdd·스킬명·절 번호를 부르지 않는다. `framework/skills/sdd/SKILL.md`(79줄)는 테스트·tdd를 일절 언급하지 않는다. r16 §1 설계(«축끼리 서로 참조하지 않고 상대 산출물 파일의 존재로만 연결») 그대로다.

### 1-2. 조건문이 2축에서 죽고 사는 것

| 문안 | 조건문 | sdd 단독 | tdd 단독 | 2축 |
|---|---|---|---|---|
| tdd 8행 | «자체 테스트를 만들어 돌렸다면» | — | 발동선 | 발동선(동일) |
| **tdd 9~10행** | **«`SPEC.md`에 그 요구에 해당하는 문장이 있으면, 그 문장을 인용해 함께 적는다»** | 죽음(파일 부재) | **죽음** — r16 1단계 실측에서 에이전트가 «SPEC.md가 없어 과제 명세문을 인용한다»고 스스로 적었다 | **산다** ← 판정 II |
| sdd frontmatter | «완전한 스펙 문서가 이미 있으면 §1 전사·§3 대조만» | 죽음 | — | **죽음 유지** — tdd 산출물은 테스트 파일이지 스펙 문서가 아니다 |
| sdd §0 | «스펙·계약 문서가 있으면 그것이 §1의 근거» | 죽음 → «요청 문장이 근거» 가지 | — | **죽음 유지**(같은 이유) |
| sdd §2 확정 절차 | «답을 받을 수 없으면 6열 표로 남기고 기본값으로 진행» | 산다(`-p` 비대화) | — | 산다(동일) |
| sdd §3 | «`선택 대기` 항목은 재확인 목록으로 사용자에게 함께 제시» | 산다 | — | 산다 → 판정 IV의 대조 재료 |

**한 가지 짚어둔 것**: tdd 9~10행이 사는 것은 **sdd가 `SPEC.md`를 먼저 쓸 때만**이다. 순서가 뒤집히면 2축에서도 죽는다 — 판정 I(순서)이 판정 II의 전제다.

**문안 수정 0건.** 고칠 지점을 찾지 못했다(두 문안 다 «판정 후 문안 수정 0건»으로 종결된 상태를 유지).

## 2. 작업 B — 통합 2축 스모크 `i1-cart-r1`

### 2-1. 런 대장

| 런 | 디렉터리 | 세션 UUID | 유효 | 비용 | 턴 | api | denials |
|---|---|---|---|---|---|---|---|
| **i1** (2축) | `i1-cart-r1/` | `17c87e5c-709c-4d58-88a1-bde43b2327bf` | **유효** | $2.63 | 37 | 505s | 0 |

- **시드**: `framework/verify-eval/seed/s2/`(r18 §3-1 조정 완료본 — `test:ac` 제거·App 문구 `TODO: implement`·`tests/` 없음)를 복사. `exp4/seed`는 동결이라 손대지 않았다. `package.json`·`package-lock.json`(2·8행) `name` → `i1-cart-r1` → **`npm ci`**(lock·package.json md5 무변경 확인) → `npm run build` 종료 0. 사전 build가 남긴 `dist/`는 `.gitignore` 대상(r31과 같은 처리).
- **요구 문안**: `sent-sd1.txt` 사본 `sent-i1.txt` — **`cmp` 동일(diff 0)**. 앱 cwd가 아니라 **부모**에 뒀다(r31 배치).
- **주입**: `--append-system-prompt "$(cat ../sdd-SKILL.md; echo; cat ../tdd-SKILL.md)"` — **sdd(79줄) → tdd(16줄) 순서**. 순서는 기록 대상이지 판정 대상이 아니다.
- **격리(r28 §5 선적용)**: cwd = `%TEMP%\fw-i1\i1-cart-r1\`. 부모의 형제는 `sent-i1.txt`·`sdd-SKILL.md`·`tdd-SKILL.md` 3파일뿐이고 **다른 앱 디렉터리는 없다**. 리포 밖이라 `framework/smoke/`의 cart SPEC 5장(= 이전 런의 정답지)이 보이지 않는다. 세션 슬러그 `C--Users-bhy99-AppData-Local-Temp-fw-i1-i1-cart-r1` — 예측값 그대로 생성.

### 2-2. 무효 규정 (r34 §2-5) — 전부 통과

| 항 | 결과 | 근거 |
|---|---|---|
| 1 `permission_denials` | **0** (`[]`) | 출력 JSON. `is_error:false`·`subtype:success` |
| 2 세션 JSONL | **존재** — 119줄 · 397,440B | 슬러그 예측 경로에 UUID 파일, 파싱 정상 |
| 3 첫 턴 무결성 | **diff 0** | JSONL 첫 사용자 메시지(487자) ↔ `sent-i1.txt` |
| 4 이웃·리포 접촉 | **0건** | 툴콜 **36건 전수가 cwd 절대 경로 안**. JSONL의 `framework` 매치 6건은 시드 `App.css`의 `.framework` CSS 클래스 — r31과 같은 무해 매치 |
| 5 `SPEC.md` 미생성 | 해당 없음 — **생성됨**(첫 Write) | |

**툴콜 순서(전사 추출, cwd 상대)**:

```
  0-4  Bash    ls·cat (시드 파악 5회)
  5    Write   SPEC.md                                    ← 첫 Write
  6-8  Write   src/CartList.tsx · CartList.css · App.tsx
  9    Write   vite.config.ts        (jsdom 환경 추가)
 10-11 Read/Edit package.json        ("test": "vitest run" 추가)
 12-15 Write   테스트 4파일
 16-17 Bash    npm run build / npm test
 18-22 Edit×4  zero-quantity 테스트 수정 → npm test 재실행
 23-31 Read/Bash/Write/Edit  oxlint · formatWon.ts 분리 · build 재확인
 32-35 Edit×3  SPEC.md (§3 대조표 등) · Grep
```

### 2-3. 판정 I~IV (사전 등록 — r34 §2-3)

| | 판정 | 값 | 근거 |
|---|---|---|---|
| **I** | 산출물 3종 | **충족 · 순서 SPEC 먼저** | `SPEC.md`(141줄) = **툴콜 #5, 첫 Write**. 구현 `src/CartList.tsx`(97줄)·`formatWon.ts`(9줄)·`App.tsx`(9줄) = #6~8·#25. 테스트 4파일 = #12~15. **SPEC → 구현 → 테스트** 순 |
| **II** | 연결 | **충족** | 4파일 **전부** 선두 헤더에 «지키는 요구 (SPEC.md §1[, §2])» + SPEC 문장 **원문 인용**. 예: `CartList.quantity-stepper.test.tsx:3-4` — `S4 "각 줄에는 수량 스테퍼가 있다 — 아이콘(기호)만 보이는 작은 버튼 두 개(−, +)이며, 버튼 안에 보이는 텍스트 라벨은 없다."` / `App.renders-cart-list.test.tsx:3` — `S1 "`src/CartList.tsx`가 CartList 컴포넌트를 정의하고, `src/App.tsx`가 CartList를 렌더한다."`. 각 `test()` 제목에도 문장 라벨을 달았다(«… (S5)», «… (S4, I4)»). verify-tdd B: 헤더 4/4 · SPEC 흔적 5건 |
| **III** | 수렴 | **남김** (13케이스 · 전건 통과) | verify-tdd A: `pass — 4파일 13케이스 (passed 13 / failed 0)`. 파일명 4개 전부 자기설명적(`CartList.zero-quantity-removes-line.test.tsx` 등) — D flags 0. **대조**: 같은 요구·sdd 단독의 `sd1-cart-r1`은 «만들고 삭제»(r25 §3-1), 문안 없는 `sd4-cart-r1`도 «만들고 삭제»(r31 §0-2, `CartList.tmpcheck.test.tsx`) |
| **IV** | 오염 | **오염 관측 — 10행 중 4행** | 아래 2-4 |

**참고 수치(축 아님 — specprobe, 판정 ③·④는 조건 5 ⓐ로 축에서 뺐다)**: `[추론]` **18** · `[MISSING` **2** / ⑤-1 범주 표 **10행**(`Clear` 1 · `Partial` 6 · `Missing` 3) / ⑤-2 미확정 표 **11행**(`선택 대기` 전체 13 인스턴스).

### 2-4. 판정 IV — `선택 대기` 행이 테스트로 굳은 경로 (첫 발현)

`SPEC.md` §2.3의 6열 표는 U1~U10 **열 행 전부가 `선택 대기`**다. 그중 **네 행의 기본값이 테스트 단언이 됐다**:

| 표 행(선택 대기) | 대응 `[추론]` 문장 | 굳힌 테스트 | 무엇이 배제됐나 |
|---|---|---|---|
| **U1** «수량 0의 화면상 의미» | I1 (줄 즉시 제거) | `zero-quantity-removes-line.test.tsx:31-43`, `:45-56` | 대안 (a) 줄 남기고 0 표시 · (b) 실행취소 스낵바 · (c) 확인 대화가 전부 **실패하게 된다** |
| **U4** «빈 장바구니 화면» | I7 | 같은 파일 `:58-69` («담은 항목이 없습니다.» + 합계 0원) | 대안 (a) 합계 줄까지 숨김 |
| **U5** «통화·금액 형식» | I8 | `line-items-and-total.test.tsx:67-72` (`formatWon(1000)==='1,000원'`) + 다른 3파일의 금액 문자열 단언 | 대안 (a) `₩12,000` (b) `Intl.NumberFormat` |
| **U6** «수량 상한» | I3 | `zero-quantity-removes-line.test.tsx:45-56` (부분 — 음수·잔존 배제) | 상한 도입 시 재작성 |

**주목할 점**: 에이전트는 이 사실을 **스스로 표기했다** — `zero-quantity-removes-line.test.tsx:5-6`의 헤더가
`- I1 "…" (SPEC.md §2.3 U1 — 기본값, 상태 `선택 대기`)`.
즉 «미확정임을 알면서 테스트로 고정»했다. 최종 응답도 U1을 (a)~(d) 선택지로 되물으면서 «(a) 즉시 제거 — 현재 구현 ✅ 권장»이라 적었다 — **테스트는 이미 (a)를 강제하고 있다.**

이것이 r22 §4 한계 ⑤·r25 §6-3이 예고하고 `docs/STATUS.md` §2-2가 «1건이라도 관측되면 §2에 인용 금지 1줄 추가 후 전후 비교(예비 개입 사전 등록)»로 걸어 둔 경로의 **첫 실측**이다. **이 라운드에서는 문안을 고치지 않는다**(r34 §0-2 — 결과를 보고 기준·문안을 바꾸지 않는다). 개입은 사전 등록된 대로 다음 라운드에서 전후 비교로 한다.

### 2-5. 부수 기록 (판정 아님)

- **최종 응답 말미의 역할 마커 누출**: `result`(1,392자) 마지막 줄이 `\n\nuser한 화면에 CartList를 두 번 렌더하면 어떻게 되나?`다 — 모델이 **사용자 턴 마커(`user`)를 흉내 내며 다음 사용자 발화를 스스로 이어 쓴** 출력 아티팩트다(JSONL 마지막 assistant 메시지에도 동일 바이트). 실측 원문이므로 **그대로 보존**하고 판정 팩 `G1.md`에도 손대지 않고 넣는다. A-4(질문 수)는 이 형태를 규정하지 않으므로 **재판정 3세션이 갈릴 수 있는 실물 고유 지점**이다 — 합성 픽스처에는 없던 형태다(§3에서 값으로 확인).
- 에이전트가 시드에 없던 실행 수단을 스스로 추가했다: `vite.config.ts`에 jsdom 환경, `package.json`에 `"test": "vitest run"`. 시드 `s2`에는 테스트 실행 스크립트가 없다(r18 §3-1이 `test:ac`를 제거했다) — tdd 문안의 «만들어 돌렸다면»을 성립시키려면 필요한 조치였고, `SPEC.md` I17이 그 사실을 적었다.
- 최종 응답이 «접근성» 문단을 따로 두고 `aria-label`·Tab·`aria-live`를 서술했다. **판정 ③은 이 스모크의 축이 아니다**(조건 5 ⓐ — 대조군 `sd4-cart-r1`에서 이미 «충족», r31 §1-2).
- `npx oxlint`는 Smart App Control 하에서도 실행됐고 경고 0으로 보고됐다(에이전트 자체 실행 — 계측값 아님).

---

## 3. 작업 C — 실물 SPEC 재판정 3세션

(진행 중 — 팩 조립·선커밋·3세션 실행 후 채운다)

---

## 4. 한계 (r34 §7 사전 등록 — 결과에 병기)

1. **n=1이다.** 판정 II·III·IV 전부 n=1 신호이지 근거가 아니다.
2. **판정 III의 «대조»는 n=1 대 n=1** — `sd1-cart-r1`(sdd 단독, 삭제)·`sd4-cart-r1`(문안 없음, 삭제) vs `i1-cart-r1`(2축, 남김)의 차이를 «tdd 덕»이라고 말할 수 없다. 같은 문안·같은 요구에서도 기본값 방향이 갈린 전례가 있다(r25 §3-3). **기록만 한다.**
3. **③④는 이 스모크의 축이 아니다** — 대조군 1런에서 이미 «③ 충족·④ 잡힘»이 나왔다(r31 §1-2).
4. 재판정 기대값이 **실험자 선판정**이라 틀릴 수 있다(§3).
5. 판정자는 **같은 계열 모델 3회 표집**이지 독립 판정자 3명이 아니다(r33 §6-1).
6. **이 라운드가 끝나도 v0.1의 효과 근거는 0이다.** 조합 연결이 확인된 것이지 조합의 실효를 잰 것이 아니다.
7. **2축 확정은 되돌리기 어렵다**(r34 §0-1 대가 1 — 실사용 2회차가 «2축판 관측»으로 소진된다).
