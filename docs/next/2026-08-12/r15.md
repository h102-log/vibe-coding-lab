# 실험 4 종료 — 다음 세션이 할 일 — r15

**상태**: **실험 4 종료.** FROZEN4 동결 → (e) 3런(전부 유효) → 측정 → `docs/REPORT5.md`까지 이 세션에서 끝냈다.
**이전**: [r14.md](r14.md) (준비 실측값) · [r13.md](r13.md) (설계 전부)
**커밋**: `056f998`(FROZEN4 동결) · `7b874e2`(3런+측정 원자료) · 이 문서와 REPORT5가 마지막 커밋에 담긴다.
브랜치 `chore/exp4-prep`, **미push** (exp4 준비~종료 전체가 이 브랜치에 있다).

---

## 1. 이 세션이 결정·관측한 것 (과정 기록)

1. **탈출구 유지 확정** (r14 §2의 미결) — 사용자 확인 후 FROZEN4 §0.3에 근거·반론과 함께 등록.
   처치 파일은 준비된 45줄 그대로 동결됐다.
2. **결과**: `mutationScore` 3/4 · 4/4 · 3/4, **중앙값 3/4 — (d)와 같다.** 주 판정 F-08은 survived 2/3.
   사전 등록 문장은 «3/4 유지» 행을 썼다 (REPORT5 §1).
3. **기전이 특정됐다**: 3런 모두 SPEC에 접근성 이름 문장이 **아예 없었다** — U-38식 명시적 제외의
   반복이 아니라 한 단계 상류(§2 침묵 지점 포착)의 갭. `todo-eb`의 F-08 killed는 S-16(한국어 문구
   불변식)의 우연 커버였다(주입 상태에서 실패 테스트 이름으로 확인).
4. **측정 인공물 1건 진단**: `todo-eb` baseline killed(1) = S-14 mtime 비교 테스트가 mutation.sh의
   복사 기반 복원과 비양립(2회 재현). 델타 판독으로 판정 영향 없음을 확인하고 FROZEN4 §11.4에 기록.
5. **UUID 중복 실측**: r13이 등록한 (e) UUID는 exp3 b2t와 동일하지만 세션 저장소가 cwd 슬러그별이라
   충돌하지 않음을 동결 전에 실측했다 (FROZEN4 §5).
6. FROZEN4 §0은 **바이트 해시**로 등재했다 (r14 §1 실측값 그대로, 방식 명기 — FROZEN2/3의 개행
   정규화와 다르니 대조 시 주의).

## 2. 다음 세션이 할 일

```
1. push + PR — 브랜치 chore/exp4-prep → main. open-pr 스킬 사용.
   (이 세션은 push 지시가 없어 로컬 커밋까지만 했다. PR 본문 재료는 REPORT5 §1·§6.)
2. 프레임워크 v0.1 설계 착수 — 실험 트랙은 전부 끝났고 남은 물음은 실험으로 못 잰다.
   재료(전부 리포트의 «다음 트랙으로 넘기는 입력» 절):
   - REPORT5 §6 — §2(침묵 지점)가 진짜 표적. §4 문안은 상한 관측으로 종결.
   - REPORT4 §6-1(재고 선구현)·§6-3(요청 모호성) — 실사용 트랙의 재료.
   - REPORT2 §6 · REPORT3 — 자산은 만들어지고(3/4) 회수된다(3/3). 자산 생명주기는 성립.
   - 부수: 환경 취약 테스트(S-14류 mtime 의존)가 자산으로 남으면 회수 시 오탐 —
     자산 이식성 검증이 프레임워크의 일이 될 수 있다 (REPORT5 §6 끝).
3. 실사용 2회차 계획 — §2 축을 문안으로 밀고 실사용으로 관측한다 (대상 프로젝트 선정 포함;
   1회차는 ai-prompt-vault였다).
```

## 3. 멈춤·미결

**없음.** 실험 트랙의 멈춤 지점은 전부 닫혔거나 발생하지 않았다 (①탈출구 닫음 · ②모델 일치 ·
③mutation.sh 수정 불필요 · ④3런 전부 테스트 남김). PR 여부·프레임워크 착수 시점은 사용자 결정.

## 4. 재현 참고 (다음에 비슷한 걸 돌릴 때)

- exp2 전수 트리 해시(`fa6c7dda…`)는 **리포 루트에서 `find exp2 …`** 로 떠야 재현된다
  (디렉터리 안에서 `find .`로 뜨면 경로 접두어가 달라 값이 다르다 — r14 값 재현에 한 번 걸렸다).
- `claude -p` 출력의 `modelUsage`에는 주 모델 외에 `claude-haiku-4-5`(하네스 보조 호출)가 같이
  찍힌다 — (d)·(e) 모두 동일했고, 멈춤 판정은 주 모델 키로만 한다.
- mutation stdout은 `tee`로 `exp4/runs/mutation-*.txt`에 남겼다 (exp2에서 유실됐던 원자료 —
  FROZEN4 §4.2에 사전 등록하고 실행).
