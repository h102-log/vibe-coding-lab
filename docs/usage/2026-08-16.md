# 2026-08-16

## 08-11 (그날 기록 — 08-12 작성)

- **Q: 「라인수 표기」 의미 → 누락** — 「md 파일 작성할때 라인수 표기해줘」(19:10) 2분 뒤 에이전트가
  AskUserQuestion으로 멈춰 «줄 번호 거터 vs 총 라인 수 카운터» 판정을 요구했다(19:12, 로그 `6983362d`).
  사건 시점 SPEC(`3894266`)의 §2에 라인수·줄 번호 문장이 없다 → **누락**.
- §2 누적: 98개 (변동 없음)
- §4.4 신호: 없음
- 비고: 사용자 답으로 «줄 번호 거터»가 확정됐으나 세션이 설계 단계에서 중단됐다(사용자 확인 —
  그만두고 방치. Read 3회·Plan 에이전트뿐, 구현·커밋 0). 결정이 §2에 명문화되지 않았고
  «커밋 안 된 §2는 존재하지 않는 것»(r8 §4.1)이라 이 침묵 지점은 그대로 남아 있다.

## 08-12 (그날 기록)

- R/Q/F: **0건** — ① S19(PDF 드랍, `41cde1c`)는 U97(08-07 기각) 번복 + 커밋된 허용 목록 수정이지만
  계기가 «생각나서, 사용 계기 없음»(사용자 확인)이라 08-04 비고 ① 선례대로 R로 세지 않는다.
  ② 「PDF 내용 검색」 요청은 확인 결과 이미 동작(추출 텍스트가 본문이 됨) — 수정 0, 문장 기록만.
  ③ S20(OCR 검색, `6119925`)은 기존 결정 번복 없는 확장.
- §2 누적: 116개 (U1–U116; 세분 불릿 포함 124문장) — 08-07 대비 +18
- §4.4 신호: 없음 — 새 문장이 구체적이다(병합은 기존 항목 우선·치환은 클립보드만·디코드 대기 등)
- 비고 ① **마일스톤 달성 — 트랙 종료 조건 충족.** 실제 프롬프트 10개 이상 + 꺼내 쓰기 1회 이상
  (사용자 확인, 08-12. r11 §2.1 기준 — 데모 아님). r11 §6 «마일스톤 또는 08-30 중 먼저» → 지금 종료
  가능하다. 하한(첫 세션 이후 3회, r11 §3.3)도 충족: 08-03 ×2 · 08-04 ×3 · 08-05 ×2 · 08-07 · 08-11 · 08-12.
  남은 것은 `docs/REPORT4.md`.
- 비고 ② **재고 소진 2건** — U97(기각 5일 만에, S19)과 U17 잔여 «가져오기/내보내기»(S21, `8bd7aee`).
  U97은 기각문이 도입 경로(pdfjs 동적 로드)까지 명시했고 S19가 그 경로대로 갔다 — 커밋(08-07 18:01) <
  요청(08-12 18:33) 순서는 git이 증명한다. **계기만 사용이었다면 R 적중이었을 사건.** S21~S23은
  사용자가 「추가 할만한 기능 … 3가지 정도 추려줘」로 **에이전트 제안을 승인**해 만든 것이라 같은 결이다.
  원 재고(r11 §1.1) 중 남은 것은 U18(서버/동기화)뿐.
- 비고 ③ **U17이 자기모순 상태다** — S21이 가져오기/내보내기를 구현했는데 §2 U17은 «나머지 항목의
  기각은 유지», §3 대조표는 «가져오기/내보내기 … UI 없음» 그대로다(`8bd7aee`). 대조표 스팟 체크가
  자기 커밋의 번복을 못 잡았다.
- 비고 ④ §3.2 «SPEC 선커밋»: 08-07 최초 준수 후 오늘 3커밋(`41cde1c`·`6119925`·`8bd7aee`) 전부
  SPEC+구현 한 커밋으로 재위반.
- 검증: `npm test` ✓ · `npm run build` ✓ (관측자 실측, 08-12)
