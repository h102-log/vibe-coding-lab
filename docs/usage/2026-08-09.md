# 2026-08-09

- **R: GFM 번복 → §2 적중(U55)** — `19cad8f`(08-04 22:34)가 «표·체크박스·취소선 같은 GFM 확장은
  렌더링하지 않는다»(U55)를 **기각**으로 커밋했고, 브라우저에서 프리뷰를 확인한 뒤 `c07a350`(22:54)이
  그 결정을 **번복**하며 `App.tsx`·`index.css`·`package.json`을 고쳤다. 이미 커밋된 코드를 고치게 만든
  요구 변화(r8 §4.1). **U55가 사건보다 먼저 커밋됐다 — git 시각 22:34 < 22:54로 순서가 증명된다.**
- **F: 프리뷰 CSS 누수 → 누락** — 전역 `ul`/`li` 셀렉터가 프리뷰 안 마크다운 목록까지 카드로 그리고
  `list-style: none`으로 불릿·번호를 지웠다. 브라우저 테스트에서 발견. 무엇이 옳은지 정해져 있지 않았다.
  **U63이 사건과 같은 커밋(`c07a350`)에 들어와 «SPEC 커밋 시각 < 사건 시각»을 만족하지 못한다 → 누락.**
- (F 아님: 라이브러리) `react-markdown`이 GFM을 기본 지원하지 않아 `remark-gfm`을 추가한 것. 도구 문제다.
- **§2 누적: 63개** — U1–U63. r12 시점 24 → 39개 증가.
- **§4.4 신호: 없음** — §2가 일반론으로 채워지지 않았다. U28(토큰 분리 기각, `git commit -m` 근거)·
  U35(본문 `#`이 이미 헤딩 문법)·U40(태그 없던 기존 항목이 안 버려짐)은 실제 침묵 지점이다.
- 비고 ① **§2 재고 3건이 사용 없이 선구현됐다.** U2(마크다운 원문 유지)·U17(검색·태그) 기각이 뒤집혔는데,
  계기는 «만들어진 걸 보고 이 기능 있으면 좋겠다»였다(사용 중 불편이 아님 → R로 세지 않는다).
  r11 §1.1이 «미리 만들면 그 관측을 스스로 없앤다»고 한 경우. 남은 재고는 U6(수정 UI)·U18(서버/동기화) 등.
- 비고 ② **r11 §3.2 «SPEC 선커밋»이 4회 연속 깨졌다** (`4a10b1e`·`610db38`·`19cad8f`·`c07a350` 모두
  SPEC+구현 한 커밋). 위 F가 누락으로 떨어진 것이 그 직접 대가다 — 2커밋이었다면 U63의 선후가 갈렸다.
- 비고 ③ 세션 수는 하한(r11 §3.3 «3회»)을 넘겼다. 08-03 밤 2회 + 08-04 3회 이상.
  마일스톤(r11 §2.1 «프롬프트 10개 + 1회 꺼내 쓰기»)은 **0/10** — 앱에 실제 프롬프트가 아직 안 들어갔다.
- 비고 ④ 대상 리포에 `.claude/skills/browser-test`(Playwright MCP)가 등록됐다(`7c23a5f`).
  실사용 환경 변화라 기록만 한다. 43줄 무결성은 유지(sha256 `00f70d94…`, 43줄).
