# REPORT5 — 실험 4: §4 문안 A/B «전부 옮겨라»

**날짜**: 2026-08-12 · **SSOT**: `exp4/FROZEN4.md` · **설계 과정**: `docs/next/2026-08-12/r13.md`·`r14.md`
**원자료**: `exp4/runs/*.json`·`exp4/runs/mutation-*.txt`·`exp4/faults-e/**/*.diff`·`exp4/runs/logprobe-*.json`
**물음** (REPORT2 §6.3에 결과를 보기 전 등록): 처치 §4에 «1·2번의 문장을 하나도 빠짐없이 테스트로
옮긴다»(B안 2줄, 43→45줄)를 더하면 exp2의 `mutationScore` 3/4(F-08 survived ×3)이 4/4가 되는가.

**결론은 FROZEN4 §8.2의 사전 등록 문장만 쓴다. 사후에 새로 쓴 문장은 없다.**

---

## 1. 1순위 `mutationScore` — 주 판정 F-08: survived 2/3

| 조건 | 앱 | baseline | F-07 | F-08 | F-03 | F-06 | **score** |
|---|---|---|---|---|---|---|---|
| (d) 43줄 | `todo-d{,b,c}` | — | killed ×3 | **survived ×3** | killed ×3 | killed ×3 | **3/4 ×3** |
| (e) 45줄 | `todo-e` | survived | killed (7) | **survived** | killed (14) | killed (5) | **3/4** |
| (e) 45줄 | `todo-eb` | killed (1)* | killed (2) | **killed (2)** | killed (10) | killed (4) | **4/4** |
| (e) 45줄 | `todo-ec` | survived | killed (2) | **survived** | killed (18) | killed (8) | **3/4** |

괄호는 실패 케이스 수. (d) 행은 exp2 실측 재게시(FROZEN2 §11.3), 재실행 없음.
\* `todo-eb`의 baseline 실패 1건은 측정 인공물(S-14, §4-5) — 델타 판독으로도 4개 결함 전부 killed 유지.

**중앙값 (d) 3/4 → (e) 3/4.** 주 판정인 F-08은 3런 중 2런에서 survived.

> **사전 등록 문장 (FROZEN4 §8.2, «(e) 3/4 유지» 행)**: «전부 옮겨라»도 에이전트의 테스트 가치
> 판단을 뒤집지 못했다 — **문안 개정의 상한 관측**. 명시적 제외 단서가 반복되는지 병기.

병기 (같은 행의 지시): **명시적 제외 단서는 반복되지 않았다.** exp2의 U-38은 «문장은 있는데 테스트에서
명시적으로 제외»였다. 이번 두 survived 런은 제외조차 아니다 — **SPEC §1·§2 어디에도 삭제 버튼의
접근성 이름 문장 자체가 없다** (3런 공통. aria 관련 문장은 필터 `aria-pressed`뿐). «전부 옮겨라»는
옮길 문장이 존재해야 작동한다 — 갭이 §4(문장→테스트 전이)에서 **§2(침묵 지점 포착)로 상류 이동**한
것이 관측의 실체다. 단일 런 `todo-eb`의 4/4는 §4-2의 진단대로 우연 커버이며, 집계(중앙값)가 4/4에
도달하지 않았으므로 4/4 행의 사전 등록 문장은 쓰지 않는다.

## 2. 처치 도달 확인 — 3/3 (2순위, 결론에 쓰지 않는다)

| 앱 | 남긴 테스트 | 케이스 | 단언 | `testsDeleted` | 탈출구(«옮기지 못한 문장과 이유») |
|---|---|---|---|---|---|
| `todo-e` | `tests/spec/todo.spec.test.tsx` | 44 | 120 | 0 | 이행 — 6건, 전부 절차성(빌드·미수정 검증류) |
| `todo-eb` | `src/todo.spec.tsx` | 36 | 100 | 0 | 이행 — 1건(러너 순환) |
| `todo-ec` | `tests/spec/spec.test.tsx` | 58 | 140 | 0 | 이행 — 5건, 전부 절차성 |

(d)의 케이스 20·28·20 대비 (e)는 44·36·58 — 추가 문안이 케이스 수를 뚜렷이 늘렸다.
탈출구 문장도 3/3이 이행했고, 제외 목록에 축 이름(영속·접근성·필터…)은 0건이다.

## 3. 비용 교환비

| | cost | turns | 벽시계 |
|---|---|---|---|
| (d) 3런 | $3.0030 · $2.5481 · $2.4352 | 38 · 36 · 47 | 710.5 · 617.2 · 607.4초 |
| (e) 3런 | $2.4984 · $3.0781 · $4.1146 | 31 · 44 · 51 | 652.8 · 765.5 · 926.3초 |
| **중앙값 비교** | $2.55 → $3.08 (**×1.21**) | 38 → 44 | 617 → 766초 (**×1.24**) |

FROZEN4 §6-2의 예상 범위(테스트 수 증가 → 비용·벽시계 증가) 안. 교환비로만 병기한다.

## 4. 병기 지표

1. **`getByRole`/`getAllByRole`**: (e) 3런 = 0/0 · 0/0 · 0/1. REPORT2의 결정적 확인(3런 0건)과
   사실상 같다 — F-08을 잡은 `todo-eb`조차 접근성 쿼리 0건이다.
2. **`todo-eb`의 F-08 killed 기전 (주입 상태에서 실패 테스트를 이름으로 확인)**: 잡은 것은
   접근성 테스트가 아니라 `S-16` «로케일 전환 컨트롤이 없고 화면 문구가 한국어 하나다» 불변식이다.
   `삭제` 텍스트 제거가 화면 문구 검사에 걸렸다 — **접근성 이름을 겨눈 커버가 아니라 우연 커버.**
3. **`firstImplOracle` 7/8 ×3** — 세 런 모두 첫 구현 제출이 AC-07(영속) 하나를 놓치고 이후 수정했다.
   `oracleTrace`: `7/8→8/8→8/8` · `0/8[RED]→7/8→8/8→8/8→8/8` ×2.
4. **F-03 용이성 상속 확인**: 실패 케이스 수 최다(14·10·18) — 4개 AC 연쇄 파괴라는 FROZEN2 §2.1의
   기록과 부합한다.
5. **측정 인공물 1건 (S-14)**: `todo-eb`의 `S-14`는 스타일 파일 mtime을 비교하는 테스트로,
   `mutation.sh`의 복원(디렉터리 복사)이 mtime을 갱신해 baseline부터 깨진다(2회 재현). 에이전트
   세션에서는 통과했다. 판정 영향 없음은 델타 판독으로 확인했다(FROZEN4 §11.4-1).
6. **주입 기록 (FROZEN2 §4.4 의무)**: 12건 diff 전부 `exp4/faults-e/`에 저장. 판단 개입 2건 —
   `todo-e`·`todo-ec`의 F-03에서 미사용이 된 `toggleTodo` import를 컴파일 가능성 유지를 위해
   함께 제거했다(FROZEN2 §2.3의 `_` 접두사와 같은 범주).

## 5. 한계

FROZEN4 §7(= FROZEN2 §7의 8항 + 2항)이 전부 상속된다. 이번 실행에서 추가로:

1. **10일 시차** — (d) 2026-08-02, (e) 2026-08-12. `modelUsage` 키는 양쪽 다 `claude-opus-5`로
   동일함을 확인했으나, 같은 문자열이 같은 가중치를 보증하지는 않는다.
2. **사전 등록 문장은 3런 동형을 전제로 쓰였다.** 혼합 결과(3/4·4/4·3/4)는 등록 시점에 예상하지
   못한 형태다. 집계는 FROZEN2 §11.3의 중앙값 방식을 상속해 3/4 행의 문장을 주 결론으로 썼고,
   4/4 단일 런은 표와 §4-2의 진단으로만 실었다.
3. **측정 절차가 산출물 테스트의 환경 가정에 민감하다** — 파일 복사 기반 복원이 mtime 의존 테스트
   (S-14)와 비양립. 이번엔 판정에 영향이 없었지만, 다른 산출물에서는 killed를 부풀릴 수 있는 유형이다.
4. **45줄 처치는 실사용 트랙(REPORT4)의 43줄과 다른 물건이다.** 섞어 해석하지 않는다.

## 6. 다음 트랙으로 넘기는 입력

**답한 것**
- §4 축의 문안 개정(실험으로 잴 수 있던 절반)은 닫혔다: «남겨라»(exp2) 위에 «전부 옮겨라»를 얹어도
  표적 커버리지는 중앙값 기준 3/4에 머문다 — **상한 관측**.
- 기전이 특정됐다: 3런 모두 SPEC에 접근성 이름 문장이 **아예 없었다**. 실패 지점은 문장→테스트
  전이(§4)가 아니라 **침묵 지점 포착(§2)**이다. exp2의 U-38(문장 있음 + 명시 제외)보다 한 단계
  상류의 실패 형태이고, REPORT4 §5의 «specHit 소수» 관측과 방향이 같다.

**답할 수 없는 것 (이 설계로는)**
- §2를 문안으로 미는 것 — 통제 실험로는 못 잰다(삼각 모순, r13 §1: 채점하려면 계약을 줘야 하고,
  계약을 주면 §2가 할 일이 사라진다). **프레임워크 v0.1 + 실사용 2회차**로 넘긴다.

**부수 입력 (프레임워크 설계 재료)**
- 환경 취약 테스트(mtime류)가 자산으로 남으면 회수 시점에 오탐을 낸다 — 자산의 이식성 검증이
  프레임워크의 일이 될 수 있다 (S-14 관측, exp3의 회수 관측과 연결).
