# 바이브 코딩 프레임워크 — 착수 전 조사 보고서

> **작성** 2026-07-30 · **목적** 자체 바이브 코딩 프레임워크 설계 착수를 위한 벤치마크 해부 및 필요 정보 식별
> **조사 방법** 벤치마크 리포 2건 직접 클론·정독 + 웹 리서치 워크플로(108 에이전트, 400만 토큰, 적대적 3표 검증)
> **모든 수치는 2026-07-30 기준.** Claude Code 문서는 빌드 단위로 갱신되고 MoAI-ADK는 조회 당일에도 push되므로, 설계 문서에 인용할 때는 커밋 SHA와 CLI 버전을 고정할 것.

---

## 내일 시작점 (TL;DR)

**확정된 것**

- HARPRAME은 벤치마킹할 오픈소스가 아니다 — 6일 된 개인 실험, **LICENSE 부재 → 코드 차용 불가**, 아이디어 참고만.
- MoAI-ADK는 npm이 아니라 **Go 단일 바이너리**(v3.0.2, Apache-2.0). npm은 0.2.29/2025-10-12에서 중단.
- **배포 형태가 지침 주입 설계를 규정한다.** 플러그인은 CLAUDE.md·rules를 배포할 수 없다.
- **MoAI-ADK는 Agent Team 오케스트레이션을 만들어서 폐기했다(Mode 3 RETIRED).** 코딩 작업 기본값은 순차 서브에이전트.
- TDD는 "지침에 적기"가 아니라 **"반증 가능하게 만들기"**가 설계의 본체다.

**다음 행동 후보 (택 1)**

1. **P1 공백 조사 2라운드** — 특히 §4의 7번(웹 도메인 결정론적 AC 상한), 8번(멀티에이전트 실익), 10번(선행 프레임워크 지형).
2. **P0만으로 최소 골격 설계 착수** — §6 권고 1·2번대로 HARPRAME 규모에서 시작.
3. **미해결 1번 자체 재현 테스트** — bypass 모드에서 훅 차단이 실제로 되는지. 이거 하나가 게이트 설계 전체를 좌우한다.

**참고 자료 위치**

- 이 문서: `RESEARCH-2026-07-30.md`
- 웹 리서치 원문(findings 15건 전문 + 반증 6건 + caveats): `research-raw-findings.md`
- 벤치마크 리포 재클론:
  ```bash
  git clone --depth 50 https://github.com/h102-log/harprame.git
  git clone --depth 50 https://github.com/modu-ai/moai-adk.git   # Windows: 경로 길이 제한으로 checkout 실패 → git ls-tree -r HEAD --name-only / git show HEAD:<path> 로 열람
  ```

---

## 0. 정정 세 가지

**1. HARPRAME은 "벤치마킹할 오픈소스 프로젝트"가 아니다.**
GitHub API 실측: 2026-07-24 생성(6일 전), 커밋 23개, **스타·포크·이슈·구독자 전부 0**, README 없음, **LICENSE 없음(`license: null`)**. 라이선스가 없으면 기본이 배타적 저작권이므로 **파일 복사·파생물 배포가 법적으로 불가**. 설계 아이디어 참고만 가능하고, 코드를 쓰려면 저자에게 문의해야 한다.

**2. moai-adk의 npm은 중단됐다.**
패키지는 존재하지만 `latest = 0.2.29`, 마지막 publish 2025-10-12. 지금은 Go 단일 바이너리(v3.0.2, Apache-2.0, **1,149 stars / 212 forks / 3,489 commits / open issues 1**, 2025-09-16 생성). "npm 배포형"은 2025년 가을 기준으로는 맞았고 현재는 무효.

**3. HARPRAME의 확장 자산은 commands 2개가 아니다.**
`.claude/settings.json`에 훅이 있다:

- `Stop` 훅: `npm run lint && npm run build && npm run test`
- `PreToolUse`(Bash) 훅: `rm -rf` / `git push --force` / `git reset --hard` / `DROP TABLE` 정규식 차단

이게 §2의 bypass 모드 문제로 이어진다.

---

## 1. 두 벤치마크의 실체

| | HARPRAME | MoAI-ADK |
|---|---|---|
| 실체 | 6일 된 **개인 설계 실험** | 프로덕션 프레임워크 |
| 지표 | 커밋 23, 스타 0, 3자 언급 0건 | 커밋 3,489, 스타 1,149, 조회 당일 push |
| 라이선스 | **없음 → 차용 불가** | Apache-2.0 |
| 규모 | 28 파일 | 6,656 파일 |
| 구현 | Python `execute.py` 533줄 | Go 1.26+ 단일 바이너리 |
| 강제 위치 | **Claude 밖** — 결정론적 외부 하네스 | **Claude 안** — 에이전트 팀 + 정책 |
| 판정 주체 | 비-LLM (exit code) | 감사 에이전트의 확률적 채점 |
| 서브에이전트 | 커스텀 정의 **없음** (내장 Explore만 사용) | 11개 카탈로그 |
| 확장 자산 | commands 2 + settings.json 훅 | agents 20 · commands 21 · skills 37 · hooks 39 · rules 67 |
| HEAD (조회 시점) | `d490047` (2026-07-26) | `cc1986cf` (2026-07-30, PR #1242) |

### HARPRAME의 실행 구조 — 가장 중요한 발견

Claude Code의 subagent/Task 도구를 **쓰지 않는다.** 대신 `claude -p --dangerously-skip-permissions --output-format json <prompt>`를 **스텝마다 새 프로세스로** 호출한다(`--resume`/`--continue`/`--session-id` 없음, 단일 호출 timeout 1800초, 정성 리뷰는 600초). 즉 컨텍스트 격리를 **서브에이전트가 아니라 "세션 분할 + 문서 자기완결성"으로** 구현한다.

정확한 표현: **계획은 네이티브 슬래시 커맨드, 스텝 실행 루프만 외부 하네스**인 하이브리드. (`harness.md` L9는 탐색 단계에서 내장 Explore 에이전트 병렬 사용을 명시한다. "네이티브 기능을 안 쓴다"는 과단정이며 웹 리서치에서 반증됐다. 없는 것은 **커스텀 서브에이전트 정의 파일과 skills 디렉터리**다.)

상태는 모델 컨텍스트가 아니라 파일시스템에 외부화:

```
phases/index.json                  # 상위 레지스트리 {"phases":[{dir,status,completed_at|failed_at|blocked_at}]}
phases/{task}/index.json           # {project, phase, steps[{step,name,status,verification_cmd,qualitative_ac,summary}]}
phases/{task}/step{N}.md           # 지시문
phases/{task}/step{N}-output.json  # 에이전트 원출력
phases/{task}/audit_log.md         # Advisory 심사 기록
```

`status`는 `pending → completed | error | blocked`. `created_at`(페이즈)·`started_at`·`completed_at`·`failed_at`·`blocked_at`은 하네스가 자동 기록하고, **에이전트는 `summary`만** 채우도록 프롬프트로 금지된다.

### HARPRAME 하네스의 5개 장치 (`execute.py`)

- **판정 주체 분리** — step 완료는 하네스가 `verification_cmd`의 exit code로 정한다. 프롬프트에 "네가 무엇을 써넣든 하네스가 무시하고 검증 결과로 덮어쓴다"고 못박음 (`execute.py:222-225`).
- **Red-Check** — 코드 작성 *전에* AC를 돌린다. 통과하면(exit 0) `assert True` 류 가짜 AC이므로 **계획을 거부하고 `sys.exit(2)`** (`_red_check`, 322행).
- **Sign-off** — 실행 전 1회, 동결된 AC 묶음을 사람에게 y/N 승인. 비용 논거: *"실행 후 코드 리뷰(수백 줄)보다 실행 전 AC 리뷰(몇 줄)가 압도적으로 싸다."*
- **Gate vs Advisory 분리** — 판별 기준 하나: *같은 입력에 항상 같은 답이 나오는가.* exit code·pytest·파일 존재 같은 결정론적 검사만 진행을 차단. LLM 정성 심사는 독립 세션에 1~5점만 받아 `audit_log.md`에 기록하고 **절대 차단하지 않는다** (`_qualitative_review`).
- **실패 지문(fingerprint)** — 연속 attempt의 검증 출력이 같으면 재시도를 끊고 **"동일 실패 반복 — 계획을 의심하라"**로 종료. 논거: 재시도는 "계획이 옳다"는 가정 위에 서 있고, 그 가정이 깨지면 주사위를 다시 굴리는 것뿐.

> ⚠️ 주의: "검증 로직의 비-LLM화가 1차 원칙"은 과단정이다(웹 리서치에서 반증). `concepts.md`가 하네스 내부 LLM을 반대하면서도 **"Advisory는 예외 — 진행을 막지 않으므로"**라고 명시한다.
> ⚠️ "AC 동결 = 파일 권한 수준의 결정론적 제약"도 과단정이다(반증). `requirements.md`가 R2 강제(AC 파일 쓰기 금지)를 **우선순위에서 뺐다**고 적는다 — 현재는 프롬프트 지시뿐이고 해시 대조는 미구현.

### MoAI-ADK의 파이프라인

`/moai plan` → `/moai run SPEC-ID` → `/moai sync SPEC-ID`. 단일 `/moai` 라우터 아래 **15개 서브커맨드**(clean, codemaps, e2e, feedback, fix, gate, goal, harness, loop, mx, plan, project, review, run, sync) + **36개 CLI 명령**. SPEC은 **Tier S/M/L**로 검증 깊이와 PR 라우팅을 결정. 자연어 입력(`/moai "fix the login bug"`)은 Analyze-First 라우팅으로 진입.

에이전트 카탈로그(11개)의 핵심 설계는 **"생성자와 검증자의 분리"**이고, 파일 수준에서 강제된다 — `sync-auditor`는 **Write/Edit 도구가 없고** `permissionMode: plan`. 채점은 Functionality 40% / Security 25% / Craft 20% / Consistency 15%.

구성: Manager 5(spec, develop, docs, git, design) + Evaluator 2(plan-auditor, sync-auditor) + builder-harness + super-advisor + e2e-tester + 읽기 전용 내장 Explore.

`.moai/` 레이아웃:

```
.moai/specs/SPEC-{ID}/{spec.md, plan.md, acceptance.md} (+ progress.md)   # 실제 파일 2,291개
.moai/brain/IDEA-{NNN}/{ideation.md, research.md, proposal.md}            # 스펙 이전 아이디어 단계
.moai/config/sections/*.yaml                                              # 31개 섹션 (quality, llm, workflow, gate, ...)
.moai/reports/ · .moai/docs/ · .moai/plans/ · .moai/state/
```

### ⚠️ "게이트"라는 같은 단어의 강도 차이

MoAI-ADK의 TRUST 5와 방법론 라우팅은 문서에 **"하드 차단 훅이 아니다"**라고 명시돼 있다. 문서화된 정책 + 에이전트 판단 + 반복 수렴 루프다. 반면 HARPRAME은 exit code로 실제로 프로세스를 멈춘다. **"강제"를 주장하려면 어느 층에서 무엇이 실제로 프로세스를 멈추는지 코드로 확인해야 한다.**

---

## 2. 플랫폼 제약 — 여기가 설계를 규정한다

조사에서 가장 실행 가능한 부분. **배포 형태를 고르는 순간 "지침을 어떻게 주입할지"가 결정된다.**

### 플러그인 배포를 고르면 잃는 것

- **플러그인 루트의 `CLAUDE.md`는 프로젝트 컨텍스트로 로드되지 않는다.** 플러그인은 skill/agent/hook으로만 컨텍스트를 기여하고, memory/instructions/rules 컴포넌트가 아예 없다. `.claude/rules/*.md`도 배포 불가.
- 그래서 **`.claude` 템플릿 리포로 설계한 워크플로는 플러그인 배포로 그대로 이식되지 않는다.** MoAI-ADK가 rules 67개를 갖고도 Go 인스톨러를 쓰는 이유일 가능성이 높다(미확인 — §5 미해결 5번).
- skill은 트리거 시 본문을 끌어오는 **lazy**(progressive disclosure), CLAUDE.md는 세션 시작 시 **eager**. → **항상 켜져 있어야 하는 규칙은 skill로 등가 대체되지 않고 `SessionStart` 훅이 필요하다.**
- 플러그인이 배포하는 subagent frontmatter는 보안상 `hooks`·`mcpServers`·`permissionMode`를 **무시**한다. 지원 필드: `name, description, model, effort, maxTurns, tools, disallowedTools, skills, memory, background, isolation`(유효값 `worktree` 하나뿐).
- `permissions.allow`는 **세션 전체**에 적용 → "특정 에이전트만 자동승인"은 플러그인 경로로 불가.
- 플러그인 에이전트는 `my-plugin:code-reviewer` 형태 스코프 이름으로 @-mention에 노출.

### 우회로

플러그인은 `hooks/hooks.json`으로 훅을 배포할 수 있고 `SubagentStart`/`SubagentStop` 매처가 `^my-plugin:db-agent$` 같은 스코프 식별자를 받는다. → **에이전트별 훅 강제는 사실상 구현 가능.**

### 훅으로 TDD를 강제할 때의 함정

- 훅 타입 5종: `command` / `http` / `mcp_tool` / `prompt`(LLM으로 프롬프트 평가, `$ARGUMENTS`) / `agent`(도구 가진 에이전틱 검증기). **`agent` 훅은 문서에 `experimental and may change` 명시** — 프레임워크 핵심 게이트로 쓰면 위험.
- 오케스트레이션·컨텍스트에 직결되는 이벤트: `SubagentStart`, `SubagentStop`, `PostToolBatch`, `PreCompact`, `PostCompact`, `TaskCreated`, `TaskCompleted`, `InstructionsLoaded`, `TeammateIdle`.
- 차단 가능 이벤트는 `plugins-reference` 표의 2종이 아니라 `hooks` 문서 기준 **약 15종**. **TDD 게이트에는 `PreToolUse`보다 `TaskCompleted` 차단이 더 적합할 수 있다.**
- 🔴 **`--dangerously-skip-permissions`(bypass) 하에서 `PreToolUse` 훅이 비동기 실행돼 차단에 실패한다는 버그 보고** — anthropics/claude-code#20946 (관련 #40117, #24327).

**그리고 이게 HARPRAME 자신의 구성과 정면 충돌한다.** HARPRAME은 `claude -p --dangerously-skip-permissions`로 에이전트를 호출하면서 `settings.json`의 `PreToolUse` 훅으로 `rm -rf`를 막으려 한다. 그 위험 명령 차단이 실제로 동작하는지 **검증되지 않았다.** 이 패턴을 그대로 베끼면 안 된다.

---

## 3. 각 축별로 확인된 것과 논쟁 중인 것

### 3.1 TDD

**확인 — 두 리포의 접근이 다르다.**

**HARPRAME은 엄밀히 말해 TDD가 아니다.** RED→GREEN→REFACTOR 강제가 없다. 있는 것은 **AC 동결 + Red-Check**: 코드 작성 전에 `verification_cmd`를 먼저 돌려 exit 0이면 가짜 AC로 판정하고 중단. 논리는 TDD의 red 단계와 같지만 층위가 다르다 — **테스트 우선이 아니라 "합격 기준 우선"**이다.

**MoAI-ADK는 조건 분기형.** 신규 또는 커버리지 10% 초과 → TDD(RED→GREEN→REFACTOR), 커버리지 10% 미만 레거시 → DDD(ANALYZE→PRESERVE→IMPROVE). 설정은 `.moai/config/sections/quality.yaml`의 `development_mode`. **브라운필드를 TDD로 밀어붙이지 않는다.** 품질 게이트는 TRUST 5(Tested 85%+, Readable 린트 0, Unified, Secured OWASP, Trackable Conventional Commits).

**`moai-workflow-tdd` 스킬의 Test-First Anti-Cheat — 훔칠 가치가 가장 큰 것:**

> **불변식 i** — 구현 전에 캡처한 RED 실패 출력의 **원문**이 완료 증거로 필수. 이 출력을 못 내놓는 실행은 RED를 건너뛴 것이므로 clean으로 보고할 수 없다.
> **불변식 ii** — 실패 테스트보다 먼저 쓰인 구현 코드는 **삭제하고 테스트부터 다시 유도**한다. "이미 구현을 썼으니 테스트가 첫 실행에 통과한다"가 바로 이 불변식이 금지하는 실패 모드다.

스킬 본문이 이유를 밝힌다 — 이전에는 RED 증거가 하나도 없어도 자기검증 매트릭스를 전부 통과시킬 수 있었고, 그러면 완료 보고서만으로 **test-first와 test-after를 구별할 수 없었다.**

**→ 교훈: "TDD를 지침에 적는 것"과 "TDD를 반증 가능하게 만드는 것"은 다른 작업이고, 후자가 설계의 본체다.**

부수 패턴(스킬마다 반복):

- **`Common Rationalizations` 표** — 에이전트가 댈 변명 ↔ 현실. 예: "구현이 돌아가면 테스트를 추가하겠다" → "사후 테스트는 코드가 *하는 일*을 검증할 뿐, *해야 할 일*을 검증하지 않는다."
- **`Red Flags`** — RED 건너뛴 흔적: 테스트 파일과 구현 파일이 같은 커밋에 있음 / 새 파일의 테스트가 첫 실행에 전부 통과 / 기능 머지 후 "add tests" 커밋 / 커버리지가 신규 기능 커밋에서 하락.
- **`Verification` 체크리스트** + `<!-- moai:evolvable-start id="..." -->` 블록으로 자기진화 영역 표시.
- **DAMP over DRY** (테스트 코드에서는 중복이 추상화보다 낫다), **Beyoncé Rule** (CI가 검증하지 않는 동작은 경고 없이 깨진다).

**논쟁/미해결** — AC 작성 주체와 코드 작성 주체를 분리하면 실제로 테스트 조작·과적합이 줄어드는지 **실증이 없다.** HARPRAME 원저자가 `requirements.md` R10에서 미해결로 명시하고 이렇게 적는다: **"'분리하면 AC 품질이 오른다'는 가정이 검증되지 않았다. 실험 없이 구현하면 비싼 추측이다."** MoAI의 plan-auditor/sync-auditor 분리가 답인지, 아니면 확률적 심판을 도입해 다른 실패 모드를 만드는지도 미검증.

### 3.2 SDD

**확인 — 성숙도 축이 두 출처에서 일치.** Böckeler(martinfowler.com, 2025-10-15)의 **spec-first / spec-anchored / spec-as-source**가 MoAI-ADK의 `moai-workflow-spec` 스킬에 그대로 구현돼 있다:

| 단계 | 의미 | 유지보수 |
|---|---|---|
| `spec-first` | 구현 후 스펙 폐기 | 없음 |
| `spec-anchored` | 구현과 함께 유지 | 분기별 리뷰 |
| `spec-as-source` | 스펙이 SSOT, 사람은 스펙만 수정 | 변경 시 구현 재생성 |

전이 규칙까지 있다: production-critical이 되면 spec-first → spec-anchored, 컴플라이언스/재생성 워크플로가 필요하면 → spec-as-source. **다운그레이드는 명시적 정당화 필요.** 스펙 표류에 대해 "전부 유지한다"는 거짓말 대신 **명시적 3단계 선택**으로 답한 것 — 내가 본 가장 정직한 해법.

**MoAI의 구체 자산:**

- **3파일 강제** `{spec.md, plan.md, acceptance.md}` + 실행 시 `progress.md`. `[HARD] 모든 SPEC 디렉터리는 3파일을 전부 가져야 한다.`
- **GEARS 표기법** (Generalized EARS, v3.0.0부터 canonical): `[Where ...][While ...][When ...] The <subject> shall <behavior>`. EARS의 `IF/THEN`을 폐기하고 `When <condition-detected>`로 통일, 주어를 "the system" 고정에서 임의 명사로 해방. `moai spec lint --strict`가 잔존 `IF/THEN`에 `LegacyEARSKeyword` 오류. **6개월 하위호환 창** 운영.
- **12개 필수 frontmatter 필드**: id, title, version, status, created, updated, author, priority, phase, module, lifecycle, tags. status enum 8종(`draft → in-progress → implemented → completed | superseded | archived | rejected`). 선택: issue_number, depends_on, lint.skip, bc_id, tier(S/M/L).
- **`[NEEDS CLARIFICATION: <topic>]` 마커** — plan.md/research.md에만 허용(spec.md/acceptance.md **금지**). 남아 있으면 plan-auditor가 "clarification gate"로 잡고, 오케스트레이터가 `AskUserQuestion`으로 해소한 뒤에야 Implementation Kickoff Approval. 3계층 구분: `[NEEDS CLARIFICATION]`(사용자 질문 필요) / `TODO`(코드 레벨 부채) / `@MX:TODO`(미테스트 코드 주석).
- **요구사항 명료화 5단계**: Step 0 가정 분석(Philosopher Framework) → Step 0.5 근본원인(Five Whys) → Step 1 범위 → Step 2 제약 추출 → Step 3 성공 기준 → Step 4 테스트 시나리오(정상·오류·경계·보안).
- **`.moai/specs/`에 들어가지 않는 것**을 표로 명시: 보안 감사·성능 리포트·의존성 분석·아키텍처 개요·API 참조·회고 → `.moai/reports/` 또는 `.moai/docs/`. `[HARD] 리포트는 존재하는 것을 분석 → reports/. SPEC은 만들 것을 정의 → specs/.`
- **Out of Scope 섹션 강제** — `### Out of Scope — <topic>` H3 + `-` 불릿이 `OutOfScopeRule` 린트를 만족.

**참고 아이디어(근거 약함)** — arXiv:2606.27045 "The Spec Growth Engine"(Grabowski, 2026-06-25): 스펙-코드 정합성을 **규율이 아니라 머지 차단 게이트**로 강제. SPEC.md 파생 **Intent Graph** vs 정적 분석 파생 **Evidence Graph**를 비교해 orphan code(스펙 소유자 없는 파일)·undeclared dependency(스펙 경계 넘는 미선언 import) 등 4종을 무조건 머지 차단 hard error. 컨텍스트는 **"Spine"**(루트→현재 노드 소유권 경로)으로 한정하고 형제 컴포넌트·의존성 내부 design·의존성 코드·전이적 의존성·임의 grep 결과를 명시적 배제 = Parnas information hiding을 에이전트 컨텍스트에 적용.

> 🔴 **미심사 단독저자 프리프린트, 경험적 평가 전무, 자사 방식 홍보 Related Work 포함.** 아이디어로만 쓰고 근거로 인용하지 말 것. 같은 논문이 GitHub Spec Kit을 "spec-anchored를 지향하나 실제로는 spec-first(스펙 1건당 약 8파일, 변경요청당 브랜치)"로 평가하는데 이것도 검증 필요. 이 논문의 Kiro "스펙 폐기" 분류는 **Kiro 공식 문서와 상충하므로 사실로 인용 금지.**

### 3.3 페이즈 분할 / 컨텍스트

**확인 — 실행 가능한 수치와 패턴:**

- MoAI 토큰 예산: **Plan 30% / Run 60% / Sync 10%**
- 컨텍스트 임계값이 **모델별**:

| 모델 클래스 | 창 | 핸드오프 임계 | 절대 상한 |
|---|---|---|---|
| Opus 5 (1M) / Opus 4.8 (1M) / GLM-5.2 (1M) | 1,000,000 | **50%** | ~500,000 |
| Fable (256K) | 256,000 | **90%** | ~230,000 |
| Sonnet/Opus 표준 (200K), Haiku (200K) | 200,000 | **90%** | ~180,000 |

  큰 창일수록 절대 여유가 크니 퍼센트 기준을 **낮게** 잡는 역설이 반영돼 있다. 95% 초과 시 다음 행동은 무조건 `/clear`.
- **Claude Code의 graduated-compaction**(Budget Reduction → Snip → Microcompact → Context Collapse → Auto-Compact)은 **소비만 하고 구현하지 않는다**고 명시. 런타임 내부는 못 건드린다. (출처: arXiv:2604.14228)
- **Verification Economy** — 검증 출력을 디스크 파일로 흘리고 컨텍스트에는 exit code + **최대 50줄 tail**만. 프롬프트 캐시 재사용(cached read 0.1×).
- **Token Circuit Breaker** — 하드 리밋(기본 90%)에서 중단, `progress.md`에 진행 저장, **붙여넣기 가능한 resume 메시지** 발급.
- 컨텍스트 사용량 추정을 `.moai/state/context-usage.json` 스냅샷 우선으로 하고, session_id/writer_pid로 유효성 검증 후 실패 시에만 바이트 휴리스틱으로 폴백.
- **HARPRAME `harness.md` 원칙 2 (축자)**: *"자기완결성 — 각 step 파일은 독립된 Claude 세션에서 실행된다. '이전 대화에서 논의한 바와 같이' 같은 외부 참조는 금지한다. 필요한 정보는 전부 파일 안에 적는다."* → **페이즈 설계 난이도를 전부 결정하는 제약.**

**HARPRAME step 설계 원칙 7개** (`harness.md` §C, 그대로 채택 가치 있음):

1. **Scope 최소화** — 한 step에서 한 레이어/모듈만. 여러 모듈을 동시에 수정해야 하면 step을 쪼갠다.
2. **자기완결성** — (위)
3. **사전 준비 강제** — 관련 문서 경로와 이전 step 산출 파일 경로를 명시.
4. **시그니처 수준 지시** — 인터페이스만 제시, 내부 구현은 에이전트 재량. 단 멱등성·보안·데이터 무결성 같은 핵심 규칙은 반드시 명시.
5. **AC는 실행 가능한 커맨드** — "~가 동작해야 한다" 금지, `npm run build && npm test` 같은 실제 커맨드.
6. **주의사항은 구체적으로** — "조심해라" 대신 **"X를 하지 마라. 이유: Y"** 형식.
7. **네이밍** — step name은 kebab-case slug (`project-setup`, `api-layer`, `auth-flow`).

**논쟁** — 🔴 컨텍스트 축소·지침 주입이 항상 이득이라는 가정에 반대 근거가 있다. arXiv:2606.20512 "Probe-and-Refine Tuning of Repository Guidance for Coding Agents"가 **LLM 생성 컨텍스트/지침 파일이 SWE-bench Lite resolve rate를 오히려 떨어뜨린다**고 보고(에이전트가 지시를 문자 그대로 따르다 역효과). 2026년 agentic-search vs semantic-index 벤치마크들도 검색 채널 강화 방향에 유리(CodeGraph류 tool call 58-70% 감소). 즉 Spine류의 강한 grep 차단이 실제 SWE 태스크에서 이득인지 **미검증·논쟁 영역**.
> 이건 3표 검증을 통과한 주장이 아니라 검증자의 부수 발견이다. **원문 재확인 없이 인용 금지.**

### 3.4 Sub-agent Team Orchestrator

**가장 중요한 발견: MoAI-ADK가 만들어서 폐기했다.**

`orchestration-mode-selection.md`에서 Mode 3(agent-team)이 **RETIRED**. 결정 트리가 절대 선택하지 않고, `--mode team` 강제 시 `MODE_TEAM_UNAVAILABLE` 센티넬을 내고 `[mode-auto-downgrade]` 로그와 함께 폴백.

| 모드 | 조건 | 동시성 |
|---|---|---|
| 1 `trivial` | 오타·한 줄, 의미 변경 없음 | 직접 실행 |
| 2 `background` | 읽기 전용 비동기 분석 | 1 |
| ~~3 `agent-team`~~ | **폐기 (tombstone)** | — |
| 4 `parallel` | **리서치** 성격 AND ≥3 도메인 | 3~5 |
| 5 `sub-agent` | **기본 폴백 — 코딩 작업 전부** | 순차 1 |
| 6 `workflow` | ≥~30파일 AND **단일 균일 변환 규칙** AND 파일 간 의존 없음 | 최대 16 (총 1000 백스톱) |

자동 선택 임계: **≥3 도메인, ≥10 파일, 또는 복잡도 점수 ≥7**.

근거로 Anthropic 문서를 직접 인용:

> *"most coding tasks involve fewer truly parallelizable tasks than research, and LLM agents are not yet great at coordinating and delegating to other agents in real time."*
> *"Start with 3-5 teammates for most workflows."*

안티패턴 목록에 **"코딩 중심 작업에 Mode 4/6 선택"**이 명시적으로 올라가 있다. Mode 6은 "scaling, not nesting" — 오케스트레이터(메인 세션)가 띄우는 fan-out이며 서브에이전트가 서브에이전트를 낳는 게 아니다.

**훔칠 만한 운영 패턴:**

- 모드 선택 근거를 `progress.md § Mode Selection`에 **grep 가능한 형식**으로 기록(입력 파라미터 블록 + 6모드 평가표 + `Decision: <mode>` 한 줄 + 정당화 2~5문장).
- **임계값 ±1 경계에서는 항상 더 단순한 모드로** 떨어뜨린다(파일 9 vs 10, 도메인 2 vs 3). Mode 6의 `~30`은 tilde-prefixed **soft** 임계 — 정확히 30이면 Mode 5로 해소.
- 경계 사례는 **Boundary Case 서브섹션**에 tie-breaker 규칙까지 남겨 사후에 임계값을 재조정.
- Mode 6/goal-loop 에이전트는 사용자에게 직접 묻지 않고 **구조화된 blocker report**를 반환 → 오케스트레이터가 `AskUserQuestion` 후 답을 주입해 재위임(비대칭 경계).

**→ 웹사이트 제작은 대체로 코딩 중심이다. 구상 4번(팀 오케스트레이터)은 방향은 맞지만 "필요에 따라"의 판정 기준을 아주 좁게 잡아야 한다.**

### 3.5 웹 도메인 — 두 리포 모두의 빈 곳

- **HARPRAME은 웹 타깃이 맞다.** `CLAUDE.md`가 `{Next.js 15}` / `{TypeScript strict}` / `{Tailwind}` 플레이스홀더 템플릿, `docs/ARCHITECTURE.md`도 `src/{app,components,types,lib,services}` 템플릿, step 템플릿 기본 AC가 `npm run build && npm test`. 하지만 **골격뿐이고 패턴 자산은 없다.**
- **MoAI의 `moai-domain-frontend` 스킬은 참조 카드다.** React 19 / Next.js 16 / Vue 3.5 패턴 목록이지만 `allowed-tools: Read, Grep, Glob` — **코드를 쓰지 않고 지식만 주입**한다. 인증·CRUD·결제·SEO의 실행 가능한 스캐폴드가 아니다. (Vercel React Best Practices 45개 규칙 8카테고리 참조, AI-Assisted Frontend Patterns에 "Playwright verification, 시각적 참조 전략" 언급은 있음.)

**→ "웹사이트 전형 패턴"은 어느 쪽도 풀지 않았다.** 차별점이 될 수 있는 동시에 근거가 가장 없는 영역.

---

## 4. 착수 전 확보해야 할 정보 체크리스트

### P0 — 첫 설계 결정 전에 필수 (대부분 확보됨)

| # | 항목 | 왜 필요한가 | 상태 / 출처 |
|---|---|---|---|
| 1 | 배포 형태 3갈래의 제약 차이 | 플러그인은 CLAUDE.md·rules를 배포 못 함 → 지침 주입 설계가 배포 형태에 종속 | ✅ 확보 · `code.claude.com/docs/en/plugins-reference` |
| 2 | 훅으로 실제 무엇을 차단할 수 있나 | "게이트"의 강도가 여기서 결정. `agent` 훅 experimental, bypass 모드 차단 실패 버그 | ⚠️ 사양 확보, **자체 재현 필요** · `/hooks`, issue #20946 |
| 3 | 플러그인 subagent frontmatter 지원 필드 | `permissionMode` 무시 → 검증자 에이전트의 파일 수준 강제가 플러그인 경로로 안 됨 | ✅ 확보 · `/sub-agents` |
| 4 | 컨텍스트 임계값과 compaction 계층 | 페이즈 경계를 어디에 그을지 결정. 런타임 내부는 못 건드림 | ✅ 확보 · 모델별 50%/90% |
| 5 | HARPRAME 라이선스 상태 | **차용 가능 여부** — 없으면 아이디어만 | ✅ 확보 · LICENSE 부재 = 불가 |
| 6 | TDD를 반증 가능하게 만드는 최소 장치 | 지침만으로는 test-after와 구별 불가 | ✅ 확보 · Red-Check + RED 출력 증거 의무화 |

### P1 — 설계는 시작할 수 있으나 확정 전 필요 (대부분 공백)

| # | 항목 | 왜 필요한가 | 어디서 얻나 |
|---|---|---|---|
| 7 | **웹 도메인 결정론적 AC의 상한선** | 인증·CRUD·폼은 Playwright/vitest exit code로 Gate 가능. 그런데 **레이아웃·반응형·접근성·SEO 메타·결제 플로는 어디까지 Gate로 승격 가능하고 어디부터 Advisory인가?** 게이트 설계 전체가 여기 걸림 | `playwright.dev/docs/test-agents`, Lighthouse/axe CLI, `code.claude.com/docs/en/chrome`, shadcn CLI — **미조사** |
| 8 | 멀티에이전트가 웹 CRUD에서 실제로 이득인가 | 구상 4번 채택 여부 + 배포 형태 선택(플러그인 frontmatter 제약 감수 가치)을 동시에 결정 | `anthropic.com/engineering/multi-agent-research-system` vs `cognition.com/blog/dont-build-multi-agents`, MoAI 실제 토큰 소비 — **URL만 확보, 미검증** |
| 9 | 컨텍스트 엔지니어링 실증 | 페이즈 분할이 정말 이득인지. **반대 근거 존재** | `research.trychroma.com/context-rot`, METR 개발자 생산성 연구, arXiv:2606.20512, arXiv:2510.20270 — **미검증** |
| 10 | 선행 프레임워크 지형 | spec-kit / BMAD-METHOD / Agent-OS / SuperClaude / Task Master / Kiro가 각각 뭘 해결하고 뭘 실패했는지. **바퀴 재발명 방지** | 비교 블로그 5건 확보(medium/arceapps/nearform/dev.to 등) — **거의 미조사** |
| 11 | 운영 실무 | 주입 파일 업데이트 시 충돌 처리, 버전 마이그레이션, 토큰 비용 추정, 체크포인트/worktree 복구, 라이선스 선택, 온보딩 | **공백** |
| 12 | 프레임워크 효과 측정 방법 | HARPRAME R7의 교훈: *"측정 없이는 개선인지 알 수 없다"* — 3줄이지만 우선순위 1번 | **공백** |

---

## 5. 미해결 질문 5개 (누구도 답을 모름)

1. **훅 기반 하드 게이트는 자동승인/bypass 모드에서 실제로 신뢰 가능한가?** HARPRAME 자신이 `--dangerously-skip-permissions`로 에이전트를 호출하면서 `PreToolUse` 훅으로 위험 명령을 막으려는 구성이 issue #20946과 정면 충돌한다. **최소 재현 테스트**(bypass 모드에서 훅 exit 2로 파괴적 명령이 실제 차단되는지, TDD 게이트로 `TaskCompleted` 차단이 `PreToolUse`보다 적합한지)를 직접 돌려야 설계 확정 가능.
2. **AC(테스트) 작성 주체와 코드 작성 주체를 분리하면 실제로 테스트 조작·과적합이 줄어드는가?** HARPRAME 원저자가 미해결로 명시, 후속 문서 없음. MoAI의 plan-auditor/sync-auditor 분리가 해답인지 아니면 확률적 심판을 도입해 다른 실패 모드를 만드는지 실증 없음.
3. **웹사이트/웹앱 도메인에서 "결정론적으로 판정 가능한 AC"를 어디까지 만들 수 있는가?** (§4 P1-7)
4. **멀티에이전트 팀 오케스트레이션이 웹 CRUD류에서 "단일 에이전트 + 페이즈 분할 + 세션 리셋"보다 실제로 이득인가?** 비용·컨텍스트 격리 정보 손실·결과 전달 오버헤드 포함 비교 근거 없음. **이 답이 구상 4번 채택 여부와 배포 형태 선택을 동시에 결정한다.**
5. **MoAI-ADK가 npm(TypeScript) → Go 단일 바이너리로 전환한 이유는?** (설치 신뢰성? 성능? 의존성 제거?) 릴리스 노트·이슈·커밋 이력에서 확인 가능하지만 미확인. **배포 형태 선택에 직접적인 교훈.**

---

## 6. 권고

1. **규모는 HARPRAME 쪽에서 시작.** 28 vs 6,656 파일. MoAI의 rules 67개·hooks 39개는 축적의 결과이지 시작점이 아니다. 그 구조를 처음부터 흉내내면 유지보수만 남는다.
2. **HARPRAME의 `requirements.md` 형식을 프레임워크 스펙 작성 규율로 채택.** 모든 항목에 `근거(실측 출처) / 이게 없으면 뭐가 깨지나 / 비용(몇 줄)`을 강제하고, 섹션을 이렇게 나눈다:
   - **A. 이미 검증된 것** — 그대로 가져간다 (비용 0줄)
   - **B. 실측이 요구한 것** — 근거가 실행 기록에 있다
   - **C. 근거가 부족한 것** — **넣기 전에 실험한다.** *"여기 있는 항목을 근거 없이 구현하면 그게 곧 추측성 설계다"*
   - **D. 알고 빼는 것** — *"없다는 걸 알고 뺀 것과 모르고 빠진 것은 다르다"*

   기능 부풀리기가 구조적으로 어려워진다. 그리고 원저자 스스로 규칙을 적어놨다: **"출처를 못 다는 항목은 요구사항이 아니라 취향이다."**
3. **팀 오케스트레이터는 마지막에.** 선행 사례가 만들어서 폐기했다. Mode 5(순차) 하나로 시작하고, 팀이 필요하다는 실측이 나올 때 붙이는 편이 안전하다.
4. **P1-7(웹 결정론적 AC 상한)을 다음 조사 라운드 1순위로.** 사용자 프레임워크의 차별점이 될 영역이고, 동시에 어느 벤치마크도 풀지 않았다.
5. **측정을 먼저 붙여라.** HARPRAME R7이 우선순위 1번으로 둔 이유: 3줄이지만 다른 모든 개선의 측정 도구다. 실제로 그들은 R4를 붙이고도 얼마나 아꼈는지 말하지 못했다(2분 49초 → 2분 39초, n=1).

---

## 7. 조사 품질에 대한 정직한 평가

**조사 각도 커버리지가 불균등하다.**

| 각도 | 상태 |
|---|---|
| A. 벤치마크 해부 | ✅ 1차 출처로 두껍게 확보 |
| C. Claude Code 확장 사양 | ✅ 1차 출처로 두껍게 확보 |
| B. 선행 프레임워크 지형 | ❌ 거의 미조사 |
| D. TDD·멀티에이전트·컨텍스트 실증 | ⚠️ 미심사 프리프린트 1편 + 부수 발견 1편뿐 |
| E. 웹 검증층 | ❌ 사실상 공백 |
| F. 운영 실무 | ❌ 사실상 공백 |

**착수 전 필요한 정보의 절반 이상이 아직 공백이다.** P0는 거의 다 채웠으니 설계 착수는 가능하지만, P1을 채우지 않고 게이트 설계를 확정하면 근거 없는 추측이 된다.

**반증된 주장 6건** — 대부분 "사실이 틀렸다"가 아니라 **"부재를 과단정했다"**는 이유로 떨어졌다. 최종 설계 문서에서 아래를 사실로 되살리지 말 것:

- ❌ "HARPRAME은 subagent·Skill을 쓰지 않는다(부재 확인)" → `harness.md`가 내장 Explore 병렬 사용을 명시하고 settings.json에 훅이 있다. 정확한 표현: **"커스텀 서브에이전트 정의 파일과 skills 디렉터리가 없다."**
- ❌ "HARPRAME 루트는 6개 항목뿐, README·LICENSE·package.json 없음" → LICENSE·README 부재는 맞지만 나머지 단정이 과했다.
- ❌ "검증 로직의 비-LLM화가 1차 원칙" → Advisory는 명시적 예외.
- ❌ "AC 동결 = 파일 권한 수준 결정론적 제약" → 현재는 프롬프트 지시뿐, 해시 대조는 미구현·후순위.
- ❌ "`harness.md`에 TDD 강제 장치 없음" → Red-Check가 `execute.py`에 있다. 단 RED→GREEN→REFACTOR 강제는 실제로 없다(§3.1).

**기타 인용 주의:**

- HARPRAME은 1차 근거는 강하지만 **"검증된 관행"의 근거로는 쓸 수 없다.** `phases/` 하위가 `0-smoke`, `1-fail` 둘뿐이고 3자 언급 0건. 리포 내부에 문서 드리프트도 있다(`harness.md` 전이 표 ↔ `execute.py` 규칙이 status 기록 주체에서 충돌 = 원저자가 R8로 인지, 참조된 `docs/weak-ac-flagging.md`는 **부재**).
- MoAI-ADK는 버전마다 에이전트 개수가 **11/24/28로 급변**한다. 수치 인용 시 SHA 고정 필수.
- arXiv:2606.27045은 미심사 단독저자 프리프린트. Kiro "스펙 폐기" 분류는 공식 문서와 상충 → 인용 금지.
- arXiv:2606.20512(컨텍스트 축소가 성능을 떨어뜨림)는 3표 검증 미통과, 검증자 부수 발견 → 원문 재확인 없이 인용 금지.

---

## 부록 A. HARPRAME 요구사항 R1~R11 요약

원저자가 실측 근거와 비용을 붙여 정리한 것. 우선순위까지 매겨져 있어 그대로 참고 가치가 있다.

**A. 검증된 것 (비용 0줄, 그대로 가져간다)**

- **R1** 판정 주체는 실행 주체 밖에 있다 — step 완료는 하네스가 exit code로. 에이전트는 `summary`만.
- **R2** Gate와 Advisory를 나눈다 — 결정론적 검증만 진행 차단.
- **R3** Red-Check — 코드 전에 AC를 돌린다. 통과하면 계획 거부. *단 약한 AC는 못 거른다 → R9.*

**B. 실측이 요구한 것 (새로 넣는다)**

- **R4** 실패 원인을 실행/계획으로 구분 기록 — 지문 같으면 재시도 중단. 약 10줄. 검증됨(3 attempt → 2 attempt).
- **R5** attempt별 산출물·검증 출력 보관 — R4의 전제. `prev_error`가 프롬프트에 넘긴 뒤 버려진다.
- **R6** 커밋은 하네스만 한다 — 에이전트가 **검증 전에** `feat:` 커밋을 남겼고, `git add -A`가 무관한 작업까지 실패 step의 커밋에 넣었다. 앞쪽은 **-1줄**(규칙 삭제), 뒤쪽(범위 제한)은 미해결.
- **R7** 개선 효과 측정 — `progress_indicator` 경과 시간이 항상 `[0s]`. `finally`가 `with` 종료 후 실행되는데 `with` 안에서 읽는다. **3줄 이동. 우선순위 1번.**
- **R8** 계획 산출물이 하네스 규칙과 모순되지 않아야 한다 — `harness.md`는 "에이전트가 status를 판정하라", `execute.py`는 "절대 수정 금지". **계획 단계가 판정 분리 원칙을 무너뜨리는 문장을 자동 삽입한다.** 근본 문제는 "규칙이 두 곳에 적혀 있다"는 구조.

**C. 근거 부족 — 넣기 전에 실험한다**

- **R9** 약한 AC 탐지 — `render_bar`의 AC가 `total=0`을 안 봤고 코드는 그 입력에서 `ZeroDivisionError`를 내면서 `completed` 도장을 받았다. **그리고 `total=0`은 `collect()`가 실제로 만드는 값이다.** *모르는 것: 무엇을 "약하다"고 판정하나. 그 판정은 결정론적인가? 아니면 Gate로 쓸 수 없다.*
- **R10** AC 작성자를 코드 작성자와 분리 — *모르는 것: "분리하면 AC 품질이 오른다"는 가정이 미검증.*
- **R11** 재계획 — *모르는 것: 재계획이 AC를 고칠 수 있는가? **고칠 수 있으면 동결이 무너지고, 못 고치면 모순 AC는 영원히 못 푼다.** 이 질문에 답하기 전에는 구현하지 않는다.*

**D. 알고 빼는 것** — 하네스 안의 LLM(Advisory 예외) / 롤백 / 부분 성공 보존.

**우선순위** — 1) R7 측정 2) R6 앞쪽 3) R8 템플릿 모순 4) R5 보관 5) R9→R10→R11 각각 **실험 먼저**.

## 부록 B. HARPRAME `concepts.md` 5개 개념의 연결 구조

```
1 하네스 = 결정론적 껍데기
      │
      ├─ 2 판정을 실행자 밖으로  ──┐
      │                          ├─→ 판정의 신뢰
      ├─ 3 AC를 실행 전에 동결  ──┘
      │
      ├─ 4 Gate/Advisory 분리    → 판정의 강도
      │
      └─ 5 재시도 / (재계획 없음) → 실패 후 복구
```

2와 3은 한 쌍이다. 판정을 밖으로 빼도(2) 기준이 사후에 휘면(3 없음) 소용없고, 기준을 얼려도(3) 실행자가 스스로 판정하면(2 없음) 얼린 의미가 없다.

**개념 2의 핵심 논거 (인용 가치 높음):** 자기심판이 나쁜 이유는 게으름이나 거짓말이 아니다. **코드를 쓴 이해와 그 코드를 검증하는 이해가 같은 이해이기 때문이다.** 이해가 틀렸으면 코드와 판정이 *같은 방향으로* 틀린다. 둘이 서로 일관돼서 안에서 보면 모순이 안 보인다. 이건 상관오류이지 부주의가 아니다 — **더 성실한 에이전트를 써도 안 풀린다.** 그래서 해법도 "더 똑똑한 심판"이 아니라 **판정 주체를 실행 주체 밖으로 빼는 것**이다.

**실제로 데인 사건:** API 조회 step에서 `WHERE` 조건이 항상 거짓이었다. 호출은 200을 반환했고 "성공"으로 기록됐다. 실제 반환은 0건. → **기계적 성공(호출됨) ≠ 의미적 성공(원하는 값).**

## 부록 C. 주요 출처

**1차 (primary)**

- `https://github.com/h102-log/harprame` — `docs/concepts.md`, `docs/requirements.md`, `.claude/commands/harness.md`, `.claude/settings.json`, `scripts/execute.py`, `phases/*/index.json`
- `https://github.com/modu-ai/moai-adk` — `README.md`, `.claude/skills/moai-workflow-{tdd,spec}/SKILL.md`, `.claude/skills/moai-domain-frontend/SKILL.md`, `.claude/rules/moai/workflow/{orchestration-mode-selection,context-window-management}.md`, `.claude/agents/moai/*`, `.moai/config/sections/*.yaml`
- `https://code.claude.com/docs/en/` — `plugins-reference`, `hooks`, `sub-agents`, `skills`, `plugin-marketplaces`, `best-practices`, `chrome`
- `https://api.github.com/repos/h102-log/harprame`, `https://api.github.com/repos/modu-ai/moai-adk`, `https://registry.npmjs.org/moai-adk`
- `https://martinfowler.com/articles/exploring-gen-ai/sdd-3-tools.html` (Böckeler, spec 성숙도 3단계)
- `https://www.anthropic.com/engineering/multi-agent-research-system`
- `https://research.trychroma.com/context-rot`
- `https://metr.org/blog/2025-07-10-early-2025-ai-experienced-os-dev-study/`
- `https://playwright.dev/docs/test-agents`
- `https://kiro.dev/docs/specs/`
- `https://arxiv.org/abs/2606.27045` (Spec Growth Engine — ⚠️ 미심사 프리프린트)
- `https://arxiv.org/abs/2510.20270`
- `https://arxiv.org/pdf/2606.20512` (⚠️ 미검증 부수 발견)
- `https://ui.shadcn.com/docs/changelog/2026-03-cli-va`

**이슈 트래커**

- `https://github.com/anthropics/claude-code/issues/20946` — bypass 모드 PreToolUse 차단 실패
- `https://github.com/anthropics/claude-code/issues/40117`, `.../24327`

**2차 (blog/secondary — 미검증)**

- `https://cognition.com/blog/dont-build-multi-agents`
- `https://nearform.com/insights/lessons-from-real-world-failures-using-spec-driven-development`
- `https://medium.com/@tim_wang/spec-kit-bmad-and-agent-os-e8536f6bf8a4`
- `https://arceapps.com/blog/sdd-frameworks-analysis-spec-kit-openspec-bmad/`
- `https://dev.to/willtorber/spec-kit-vs-bmad-vs-openspec-choosing-an-sdd-framework-in-2026-d3j`
- `https://simonwillison.net/guides/agentic-engineering-patterns/red-green-tdd/`
- `https://deepwiki.com/modu-ai/moai-adk`, `https://deepwiki.com/github/spec-kit/10.1-create-new-feature`
