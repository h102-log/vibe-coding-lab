# SPEC — CartList (담은 항목 목록)

작업 문서. 구현 산출물이 아니다.

**스펙 소스 판별(§0):** 계약·요구사항 문서 없음. 저장소에는 vite React 템플릿과
`package.json`만 있고 장바구니에 관한 기존 문서·코드는 없다. 따라서 **요청 문장이 근거**이고,
§2가 찾아낸 침묵은 초안 기본값으로 채워 진행하며 확정은 사용자에게 되돌린다.

---

## 1. 명시된 것

| # | 검증 가능한 문장 | 근거 |
|---|---|---|
| S1 | `src/CartList.tsx`는 컴포넌트 `CartList`를 내보낸다. | 요청 2문단 |
| S2 | `src/App.tsx`의 `App`은 렌더 결과 안에 `CartList`를 포함한다. | 요청 2문단 |
| S3 | 담긴 항목이 N개일 때 `CartList`는 N개의 줄을 렌더한다. | 요청 1 |
| S4 | 각 줄은 항목 이름·단가·수량·소계 네 값을 모두 표시하고, 소계는 그 줄의 `수량 × 단가`와 같다. | 요청 1 |
| S5 | 각 줄에 − 버튼과 + 버튼이 각각 하나씩 있다. 두 버튼은 글리프(−, +)만 보이고 텍스트 라벨이 보이지 않으며, 줄의 본문 텍스트보다 작은 치수를 갖는다. | 요청 2 |
| S6 | 어떤 줄의 + 를 누르면 그 줄의 수량이 정확히 1 증가하고, 다른 줄의 수량은 변하지 않는다. | 요청 3 |
| S7 | 어떤 줄의 − 를 누르면 그 줄의 수량이 정확히 1 감소한다. | 요청 3 |
| S8 | 수량이 0에 도달한 줄은 이후 렌더되지 않고 전체 합계에도 기여하지 않는다. | 요청 4 + D1 |
| S9 | 목록 아래에, 담긴 모든 줄의 소계를 더한 값이 "전체 합계"로 표시된다. | 요청 5 |
| S10 | 시작 데이터는 `CartList` 모듈 안에 하드코딩된 3개 항목이다. | 요청 6 |
| S11 | `npm run build`(= `tsc -b && vite build`)가 종료 코드 0으로 끝난다. | 요청 완료 조건, `package.json:8` |

---

## 2. 명시되지 않은 것

### 2-1. 기본값으로 확정한 문장 (모두 `[추론]`)

| # | 검증 가능한 문장 | 출처 |
|---|---|---|
| I1 | 수량이 1인 줄에서 − 를 누르면 그 줄은 목록에서 사라진다(수량 0 = 목록에서 제거). 수량은 음수가 되지 않는다. | [추론] D1 |
| I2 | 담긴 줄이 0개일 때 목록 자리에 "담은 항목이 없습니다."가 표시되고, 전체 합계는 ₩0으로 표시된다. | [추론] D2 |
| I3 | 사라진 줄을 되살리는 조작은 없다(되돌리기·복구 UI 없음). | [추론] D1 |
| I4 | − / + 버튼은 각각 접근 가능한 이름 `"{항목 이름} 수량 줄이기"` / `"{항목 이름} 수량 늘리기"`를 가지며, 글리프는 보조기술에서 숨겨진다. | [추론] D5 |
| I5 | 두 버튼은 네이티브 `<button type="button">`이므로 Tab으로 도달하고 Enter/Space로 눌린다. 커스텀 tabindex·키 핸들러를 두지 않는다. | [추론] D5 |
| I6 | 금액(단가·소계·합계)은 `Intl.NumberFormat('ko-KR', {style:'currency', currency:'KRW'})` 형식(예: `₩18,000`)으로 표시된다. | [추론] D6 |
| I7 | 단가와 수량은 0 이상의 정수이므로 소계·합계에 반올림이 필요한 소수는 발생하지 않는다. | [추론] D6 |
| I8 | 수량 상한은 없다. + 는 몇 번이든 눌릴 수 있고 비활성화되지 않는다. | [추론] D7 |
| I9 | 각 항목은 이름과 무관한 안정적인 `id`를 갖고, 리스트 key는 `id`다. 이름이 같은 항목 둘이 있어도 별개 줄로 다뤄진다. | [추론] D8 |
| I10 | 항목의 렌더 순서는 시작 데이터 순서이고, 수량 변경·줄 제거가 남은 줄의 순서를 바꾸지 않는다. | [추론] D8 |
| I11 | 장바구니 상태는 `CartList` 내부 `useState`에 있고 `CartList`는 필수 prop이 없다. `App`은 `<CartList />`로 렌더한다. | [추론] D9 |
| I12 | 수량이 4개 값의 한 줄 안에서 보이며, 화면 표시는 `2개`, 보조기술 낭독은 `수량 2개`다. | [추론] D5 |
| I13 | 스타일은 `src/CartList.css`에 두고 `CartList.tsx`가 import한다. 기존 `index.css`의 CSS 변수(`--border`, `--text-h`, `--accent` 등)를 재사용하고 기존 파일은 수정하지 않는다. | [추론] D10 |
| I14 | 수량 변경 시 aria-live 알림은 두지 않는다(변경된 숫자·소계·합계는 화면 텍스트로만 갱신된다). | [추론] D11 |
| I15 | 서버·저장소 연동이 없다. 새로고침하면 시작 데이터로 돌아간다. | [추론] D12 |
| I16 | 자동 테스트 파일은 추가하지 않는다. 완료 신호는 `npm run build`의 종료 코드 0뿐이다. | [추론] D3 |

`[MISSING: 실제 장바구니 데이터 출처]` — 하드코딩 3항목이 나중에 무엇으로 대체되는지(props / fetch /
전역 스토어) 요청이 말하지 않는다. I11의 "prop 없음"은 이 답에 따라 뒤집힐 수 있다.

`[MISSING: 디자인 기준]` — 색·간격·타이포의 기준 시안이 없다. 기존 템플릿 CSS 변수에 맞춘 것이 전부다.

### 2-2. 점검표

| 범주 | 상태 |
|---|---|
| 1. 기능 범위와 동작 | Partial |
| 2. 도메인·데이터 모델 | Partial |
| 3. 상호작용·UX 흐름 | Partial |
| 3a. 접근성(이름·키보드 도달성) | Missing |
| 4. 비기능 품질 속성 | Missing |
| 5. 통합·외부 의존 | Missing |
| 6. 엣지 케이스·실패 처리 | Missing |
| 7. 제약·트레이드오프 | Partial |
| 8. 용어·일관성 | Partial |
| 9. 완료 신호 | Clear |
| 10. 기타·미정 자리 | Missing |

### 2-3. 침묵 지점과 적용한 기본값

답을 받을 수 없는 상태에서 기본값으로 진행했다. 아래 행은 질문이 아니라 기록이다.

| # | 침묵 지점 | 적용한 기본값 | 대안 | 상태 | 번복 조건 |
|---|---|---|---|---|---|
| D1 | 요청 4 "더 이상 담긴 것이 아니다"의 해석 — 줄을 제거하는가, 줄은 남기고 "담기지 않음" 상태로 표시하는가 | 수량 0 = 목록에서 제거. 수량 1에서 − 를 누르면 줄이 사라진다 | (a) 줄을 남기고 흐리게 + "다시 담기" 버튼 (b) 수량 1에서 − 를 비활성화해 0에 도달 불가 | 선택 대기 | 실수로 지운 항목을 되살리려는 요구가 나오면 (a)로 전환 |
| D2 | 모든 줄이 사라진 뒤 화면 | "담은 항목이 없습니다." + 합계 ₩0 | 목록 영역 자체를 숨김 / 추천 상품 표시 | 선택 대기 | 빈 상태에서 이어질 다음 동작(계속 쇼핑 등)이 정해지면 |
| D3 | vitest·jsdom·Testing Library가 설치돼 있는데 테스트를 요구하지 않음 (`package.json`에 `test` 스크립트도, jsdom 설정도 없음) | 테스트 파일을 추가하지 않음 | 스테퍼 동작·합계·0 제거를 덮는 `CartList.test.tsx` + `test` 스크립트 + vitest jsdom 설정 추가 | 선택 대기 | 회귀 방지가 필요해지는 순간(스테퍼 로직 재수정 요청 등) |
| D4 | 마크업 구조 — 목록인가 표인가 | `<ul>/<li>`, 각 값은 `<span>` + 보조기술용 라벨 | `<table>` + `<th scope="col">`(이름·단가·수량·소계 4열이라 표가 더 맞을 수 있음) + `<tfoot>` 합계 | 선택 대기 | 열이 늘거나 정렬·열 머리글이 필요해지면 표로 전환 |
| D5 | 아이콘만 있는 버튼의 접근 가능한 이름, 수량의 낭독 형태 | I4·I5·I12 | 버튼에 `aria-label` 없이 `title`만 / 수량을 `<input type="number">`로 | 선택 대기 | 스크린리더 검수 결과가 오면 |
| D6 | 통화·로케일·자릿수 | `₩` + ko-KR 천 단위 구분, 정수 원 | 숫자만 표시 / 다국어 통화 / 소수 둘째 자리 | 선택 대기 | 원화 아닌 통화나 소수 단가가 들어오면 |
| D7 | 수량 상한 | 없음 | 재고 수량까지 / 고정 상한(예: 99)에서 + 비활성화 | 선택 대기 | 재고 개념이 도입되면 |
| D8 | 항목 식별자와 정렬 | `id: number`, 시작 순서 유지 | 이름을 key로 / 이름·가격 정렬 옵션 | 선택 대기 | 서버 데이터의 실제 id 타입이 정해지면 |
| D9 | 상태 소유자와 prop 경계 | `CartList` 내부 `useState`, 필수 prop 없음 | `items`/`onChange`를 prop으로 받는 제어 컴포넌트, 초기값만 `initialItems`로 주입 | 선택 대기 | 합계나 항목을 `App`의 다른 부분이 함께 써야 하면 |
| D10 | 스타일 위치 | 새 `src/CartList.css` | 기존 `App.css`에 추가 / CSS Modules / 인라인 style | 선택 대기 | 프로젝트 스타일 규약이 제시되면 |
| D11 | 수량 변경의 보조기술 알림 | aria-live 없음 | 합계나 수량에 `role="status"` | 선택 대기 | 알림 누락이 문제로 보고되면 |
| D12 | 지속성 | 없음(새로고침하면 초기화) | localStorage / 서버 저장 | 선택 대기 | 세션 간 유지 요구가 생기면 |

**질문 승격(최대 3개)** — 답에 따라 구현이 가장 크게 갈리는 것: **D1**(0 수량 = 제거 vs 표시 유지),
**D4**(목록 vs 표), **D3**(테스트 추가 여부). 나머지는 기본값이 충분하고 되돌리기 싸서 `[추론]`으로만 기록했다.

---

## 3. 완료 전 대조

구현 후 각 문장을 참으로 만드는 코드 위치를 지목한다.

| # | 지목 위치 | 판정 |
|---|---|---|
| S1 | `src/CartList.tsx:24` `export default function CartList()` | 충족 |
| S2 | `src/App.tsx:1` import + `src/App.tsx:7` `<CartList />` | 충족 |
| S3 | `src/CartList.tsx:48-49` — `items.map(...)` → `<li>` | 충족 |
| S4 | 이름 `src/CartList.tsx:50` / 단가 `:51-54` / 수량 `:64-67` / 소계 `:77-80`(`item.unitPrice * item.quantity`, `:79`) | 충족 |
| S5 | `src/CartList.tsx:56-63`(−), `:68-75`(+) — 자식이 글리프 `<span>` 하나뿐(`:62`, `:74`); 치수 `src/CartList.css:46-48`(`1.55rem` = 본문 18px보다 작음) | 충족 |
| S6 | `src/CartList.tsx:72` `changeQuantity(item.id, 1)` → `:30-34`(id 불일치 줄은 `:31`에서 그대로 반환) | 충족 |
| S7 | `src/CartList.tsx:60` `changeQuantity(item.id, -1)` → `:32` `item.quantity + delta` | 충족 |
| S8 | `src/CartList.tsx:33` — `quantity <= 0`이면 `[]` 반환(제거); 합계는 남은 `items`만 더함(`:38`) | 충족 |
| S9 | `src/CartList.tsx:38` `total` + `:86-89` 목록(`:47-83`) 뒤 렌더 | 충족 |
| S10 | `src/CartList.tsx:12-16` `INITIAL_ITEMS` 3개 | 충족 |
| S11 | `npm run build` → `EXIT=0` (2026-08-14 실행 확인) | 충족 |
| I1 | `src/CartList.tsx:32-33` — 1에서 −1 → 0 → `[]` | 충족 |
| I2 | `src/CartList.tsx:44-45` 빈 문구 분기, `:86-89` 합계는 항상 렌더(빈 배열 → `₩0`) | 충족 |
| I3 | 되살리기 코드 없음 | 충족(부재로 충족) |
| I4 | `src/CartList.tsx:59,71` `aria-label`; `:62,74` `aria-hidden="true"` 글리프 | 충족 |
| I5 | `src/CartList.tsx:57,69` `type="button"`; `tabIndex`·`onKeyDown` 없음 | 충족 |
| I6 | `src/CartList.tsx:18-22` `krw` 포매터, 사용처 `:53,79,88` | 충족 |
| I7 | `src/CartList.tsx:5-10` 타입 + `:12-16` 정수 데이터 | 충족 |
| I8 | `src/CartList.tsx:68-75` — `disabled`·상한 분기 없음 | 충족(부재로 충족) |
| I9 | `src/CartList.tsx:6` `id: number`, `:49` `key={item.id}` | 충족 |
| I10 | `src/CartList.tsx:29-35` `flatMap`이 순서 보존, 정렬 코드 없음 | 충족 |
| I11 | `src/CartList.tsx:24` 매개변수 없는 시그니처, `:25` `useState`; `src/App.tsx:7` | 충족 |
| I12 | `src/CartList.tsx:64-67`(sr-only `수량 ` + `{quantity}개`) + `src/CartList.css:73-79`, `:109-119` | 충족 |
| I13 | `src/CartList.tsx:2` `import './CartList.css'`; `src/index.css`·`src/App.css` 무수정 | 충족 |
| I14 | `aria-live`·`role="status"` 없음 | 충족(부재로 충족) |
| I15 | `fetch`·`localStorage` 없음 | 충족(부재로 충족) |
| I16 | 저장소에 테스트 파일 없음 | 충족(부재로 충족) |

**추가 확인(문서에 남기는 기록).** S3~S9·I1·I2·I5·I10을 임시 테스트 파일로 실제 렌더·클릭해 통과를 확인한
뒤(`vitest run --environment jsdom`, 1 passed) 그 파일은 삭제했다 — D3의 기본값(테스트 파일 미포함)을
유지하기 위한 것이고, 저장소에는 남아 있지 않다. D3을 "추가"로 확정하면 이 검증을 정식 테스트로 되살린다.

**재확인 목록** — §2-3의 12개 행이 모두 `선택 대기`다. 그중 D1·D4·D3이 답에 따라 구현이 바뀌는 항목이다.
