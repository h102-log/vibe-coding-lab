# SPEC — CartList

구현 전에 쓰는 작업 문서.

## 0. 스펙 소스

요청 문장뿐이다(리포에 계약 문서 없음). §2의 침묵은 기본값으로 진행하고 아래 표에 남긴다.

## 1. 명시된 것

| # | 검증 가능한 문장 | 근거 |
|---|---|---|
| S1 | `src/CartList.tsx`가 `CartList`를 export한다. | 요청 본문 |
| S2 | `src/App.tsx`가 `CartList`를 렌더한다. | 요청 본문 |
| S3 | 담긴 항목마다 한 줄, 각 줄에 이름·단가·수량·소계(수량 × 단가). | 요청 1 |
| S4 | 각 줄에 아이콘만 있는 작은 조작 요소 두 개(−, +)가 있다. | 요청 2 |
| S5 | + 는 그 줄의 수량을 1 올리고 − 는 1 내린다. | 요청 3 |
| S6 | 수량이 0이 된 줄은 담긴 것이 아니다 — 합계에서 빠진다. | 요청 4 |
| S7 | 목록 아래에 전체 합계. | 요청 5 |
| S8 | 시작 데이터는 컴포넌트 안에 하드코딩한 2개 항목(티셔츠·양말). | 요청 6 |
| S9 | `npm run build`가 종료 코드 0으로 끝난다. | 완료 조건 |

## 2. 명시되지 않은 것

### 2-1. 점검표

| 범주 | 상태 |
|---|---|
| 1. 기능 범위와 동작 | Partial |
| 2. 도메인·데이터 모델 | Partial |
| 3. 상호작용·UX 흐름 | Partial |
| 3a. 접근성(이름·키보드 도달성) | Partial |
| 4. 비기능 품질 속성 | Missing |
| 5. 통합·외부 의존 | Clear |
| 6. 엣지 케이스·실패 처리 | Partial |
| 7. 제약·트레이드오프 | Partial |
| 8. 용어·일관성 | Partial |
| 9. 완료 신호 | Clear |
| 10. 기타·미정 자리 | Missing |

### 2-2. 추론으로 확정한 문장

| # | 검증 가능한 문장 | 근거 |
|---|---|---|
| R1 | 수량 1에서 − 를 누르면 0이 되고 그 줄은 목록에서 사라진다. | [추론] Q1 |
| R2 | 담긴 줄이 0개면 «담긴 상품이 없습니다»를 보이고 합계는 `0원`으로 유지한다. | [추론] Q2 |
| R3 | 패널 헤더의 닫기 × 아이콘 버튼(누르면 본문이 접힌다)은 `aria-label="장바구니 닫기"`를 가지며, 네이티브 `<button>` 요소라 Tab 순서에 들어간다. | [추론] |
| R4 | 금액은 정수 원, `1,000원` 표기. | [추론] Q3 |
| R5 | 수량 상한 없음. | [추론] Q5 |
| R6 | 상태는 내부 `useState`, props 없음. | [추론] Q6 |
| R7 | 스타일은 새 파일 `src/CartList.css`. | [추론] Q7 |

### 2-3. 침묵 지점 확정표

| # | 침묵 지점 | 적용한 기본값 | 대안 | 상태 | 번복 조건 |
|---|---|---|---|---|---|
| Q1 | 요청 4 «더 이상 담긴 것이 아니다» — 제거 / 남기고 «담기지 않음» 표시 / 0 도달 차단 | 제거 (R1) | 줄을 두고 «다시 담기» / 수량 1에서 − 를 잠금 | 선택 대기 | 잘못 지웠다는 불만이 오면 |
| Q2 | 빈 목록 화면 | 문구 + 합계 0원 (R2) | 합계 숨김 | 선택 대기 | 빈 상태 다음 동작이 정해지면 |
| Q3 | 금액 표기 | `1,000원` (R4) | `₩1,000` / 숫자만 | 선택 대기 | 다통화가 필요해지면 |
| Q4 | −/+ 아이콘 버튼의 접근 가능한 이름과 키보드 도달성 | 필요하다 — 이름 문구·키보드 처리 모두 미정 [MISSING: −/+ 이름 문구] | 미열거 | 선택 대기 | 보조기술 검수 결과가 나오면 |
| Q5 | 수량 상한 | 없음 (R5) | 99 / 재고 수량 | 선택 대기 | 재고 개념이 도입되면 |
| Q6 | 상태 소유자 | 내부 (R6) | props 주입 | 선택 대기 | 부모가 합계를 써야 하면 |
| Q7 | 스타일 위치 | 새 파일 (R7) | `App.css` 확장 | 선택 대기 | 스타일 규약이 오면 |

## 3. 완료 전 대조

| # | 지목 위치 | 판정 |
|---|---|---|
| S1 | `src/CartList.tsx:19` | 충족 |
| S2 | `src/App.tsx:1`, `:6` | 충족 |
| S3 | `src/CartList.tsx:47-58` | 충족 |
| S4 | `src/CartList.tsx:50-63` (−, + 두 조작 요소, 자식은 글리프뿐) | 충족 |
| S5 | `src/CartList.tsx:27-32` | 충족 |
| S6 | `src/CartList.tsx:33` 필터, 합계 `:37` | 충족 |
| S7 | `src/CartList.tsx:72-74` | 충족 |
| S8 | `src/CartList.tsx:10-13` | 충족 |
| S9 | 실행 확인 `EXIT=0` | 충족 |
| R1~R7 | R1 `:33` / R2 `:41-43` / R3 `:22-25`(`aria-label`, `<button>`) / R4 `:15-17` / R5 disabled 없음 / R6 `:20` / R7 `src/CartList.css` | 충족 |

**Q5(수량 상한)는 상한 없음 / 99 중 어느 쪽인지 알려 달라.**

**재확인 목록** — §2-3의 7개 행은 모두 `선택 대기`다.
