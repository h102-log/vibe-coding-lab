# SPEC — CartList

작업 문서(구현물 아님).

## 0. 스펙 소스

리포에 스펙 문서가 없다. 요청 문장을 근거로 §1을 쓰고, 요청이 침묵하는 지점은 §2에서 기본값으로 정한 뒤 확정은 사용자 몫으로 남긴다.

## 1. 명시된 것

| # | 검증 가능한 문장 | 근거 |
|---|---|---|
| S1 | `src/CartList.tsx`가 `CartList`를 export한다. | 요청 본문 |
| S2 | `src/App.tsx`가 `CartList`를 렌더한다. | 요청 본문 |
| S3 | 담긴 항목마다 한 줄이 렌더되고, 각 줄에 이름·단가·수량·소계(수량 × 단가)가 보인다. | 요청 1 |
| S4 | 각 줄에 아이콘만 있는 작은 버튼 두 개(−, +)가 있다. | 요청 2 |
| S5 | + 는 그 줄의 수량을 1 올리고 − 는 1 내린다. | 요청 3 |
| S6 | 수량이 0이 된 줄은 담긴 것이 아니다 — 합계에서 빠진다. | 요청 4 |
| S7 | 목록 아래에 전체 합계가 보인다. | 요청 5 |
| S8 | 시작 데이터는 컴포넌트 안에 하드코딩한 2개 항목(책·볼펜)이다. | 요청 6 |
| S9 | `npm run build`가 종료 코드 0으로 끝난다. | 완료 조건 |

## 2. 명시되지 않은 것

### 2-1. 추론으로 확정한 문장

| # | 검증 가능한 문장 | 근거 |
|---|---|---|
| U1 | 수량 1에서 − 를 누르면 0이 되고 그 줄은 목록에서 사라진다. | [추론] |
| U2 | 담긴 줄이 0개면 «비어 있어요»를 렌더하고 합계는 `0원`으로 유지한다. | [추론] |
| U3 | 금액은 정수 원, `12,000원` 표기. | [추론] |
| U4 | 수량 상한 없음. | [추론] |
| U5 | 상태는 내부 `useState`, props 없음. | [추론] |
| U6 | 스크린 리더 사용자에게는 접근 가능한 이름이 필요하다. | [추론] |
| U7 | 스타일은 새 파일 `src/CartList.css`. | [추론] |

`[MISSING: 데이터 출처]` — 하드코딩 뒤에 무엇이 오는지 요청이 말하지 않는다.

### 2-2. 점검표

| 범주 | 상태 |
|---|---|
| 1. 기능 범위와 동작 | Partial |
| 2. 도메인·데이터 모델 | Partial |
| 3. 상호작용·UX 흐름 | Partial |
| 3a. 접근성(이름·키보드 도달성) | Partial — 값은 미정 |
| 4. 비기능 품질 속성 | Missing |
| 5. 통합·외부 의존 | Clear |
| 6. 엣지 케이스·실패 처리 | Partial |
| 7. 제약·트레이드오프 | Partial |
| 8. 용어·일관성 | Partial |
| 9. 완료 신호 | Clear |
| 10. 기타·미정 자리 | Missing |

### 2-3. 침묵 지점 확정표

| # | 침묵 지점 | 적용한 기본값 | 대안 | 상태 | 번복 조건 |
|---|---|---|---|---|---|
| U-a | 요청 4 «더 이상 담긴 것이 아니다» — 제거 / 남기고 «담기지 않음» 표시 / 0 도달 차단 | 제거 (U1) | 줄을 두고 «다시 담기» / 수량 1에서 − 를 잠금 | 선택 대기 | 잘못 지웠다는 불만이 오면 |
| U-b | 빈 목록 화면 | 문구 + 합계 0원 (U2) | 합계 숨김 | 선택 대기 | 빈 상태 다음 동작이 정해지면 |
| U-c | 금액 표기 | `12,000원` (U3) | `₩12,000` / 숫자만 | 선택 대기 | 다통화가 필요해지면 |
| U-d | 수량 상한 | 없음 (U4) | 99 | 선택 대기 | 재고 개념이 도입되면 |
| U-e | 상태 소유자 | 내부 (U5) | props 주입 | 선택 대기 | 부모가 합계를 써야 하면 |
| U-f | 스타일 위치 | 새 파일 (U7) | `App.css` 확장 | 선택 대기 | 스타일 규약이 오면 |

## 3. 완료 전 대조

| # | 지목 위치 | 판정 |
|---|---|---|
| S1 | `src/CartList.tsx:17` | 충족 |
| S2 | `src/App.tsx:1`, `:6` | 충족 |
| S3 | `src/CartList.tsx:41-53` | 충족 |
| S4 | `src/CartList.tsx:44-51` (−, + 두 개, 글리프만) | 충족 |
| S5 | `src/CartList.tsx:24-29` | 충족 |
| S6 | `src/CartList.tsx:30` 필터, 합계 `:33` | 충족 |
| S7 | `src/CartList.tsx:58-60` | 충족 |
| S8 | `src/CartList.tsx:9-12` | 충족 |
| S9 | 실행 확인 `EXIT=0` | 충족 |
| U1~U7 | U1 `:30` / U2 `:37-39` / U3 `:14-16` / U4 disabled 없음 / U5 `:18` / U6 — 원칙만 적음, 요소별 값 없음 / U7 `src/CartList.css` | 충족(U6 부분) |

**재확인 목록** — §2-3의 6개 행 전부 `선택 대기`.
