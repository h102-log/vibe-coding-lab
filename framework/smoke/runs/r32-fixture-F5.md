# SPEC — CartList (담은 항목 목록)

작업 문서 — 2026-08-14 초안.

## 0. 스펙 소스

계약·요구사항 문서가 없어 요청 문장이 유일한 근거다. 요청이 침묵하는 지점은 §2에서 기본값으로 확정하고 아래 표에 기록한다.

## 1. 명시된 것

| # | 검증 가능한 문장 | 근거 |
|---|---|---|
| S1 | `src/CartList.tsx`가 `CartList` 컴포넌트를 정의해 내보낸다. | 요청 본문 |
| S2 | `src/App.tsx`는 `CartList`를 렌더한다. | 요청 본문 |
| S3 | 담긴 항목이 1개 이상이면 각 항목이 한 줄로 렌더되고, 각 줄은 이름·단가·수량·소계를 보여준다(소계 = 수량 × 단가). | 요청 1 |
| S4 | 각 줄에 아이콘만 있는 작은 버튼 두 개(−, +)로 된 수량 스테퍼가 있다. | 요청 2 |
| S5 | + 는 그 줄의 수량을 정확히 1 올리고, − 는 정확히 1 내린다. | 요청 3 |
| S6 | 수량이 0이 된 줄은 담긴 것이 아니다 — 전체 합계에서 빠진다. | 요청 4 |
| S7 | 목록 아래에 전체 합계가 표시된다. | 요청 5 |
| S8 | 시작 데이터는 컴포넌트 안에 하드코딩한 2개 항목(우유·빵)이다. | 요청 6 |
| S9 | `npm run build`가 종료 코드 0으로 끝난다. | 완료 조건 |

## 2. 명시되지 않은 것

### 2-1. 점검표

| 범주 | 상태 |
|---|---|
| 1. 기능 범위와 동작 | Partial |
| 2. 도메인·데이터 모델 | Partial |
| 3. 상호작용·UX 흐름 | Partial |
| 3a. 접근성(이름·키보드 도달성) | Missing |
| 4. 비기능 품질 속성 | Missing |
| 5. 통합·외부 의존 | Clear |
| 6. 엣지 케이스·실패 처리 | Partial |
| 7. 제약·트레이드오프 | Missing |
| 8. 용어·일관성 | Partial |
| 9. 완료 신호 | Partial |
| 10. 기타·미정 자리 | Missing |

### 2-2. 추론으로 확정한 문장

| # | 검증 가능한 문장 | 근거 |
|---|---|---|
| T1 | 수량 1에서 − 를 누르면 0이 되고 그 줄은 목록에서 즉시 사라진다. | [추론] |
| T2 | 담긴 줄이 0개면 목록 대신 «담긴 상품이 없어요»를 렌더하고 합계 줄은 렌더하지 않는다. | [추론] |
| T3 | 금액은 정수 원화이며 `1,900원`처럼 표기한다. | [추론] |
| T4 | 수량 상한은 없다. | [추론] |
| T5 | 접근성을 고려한다(색 대비·글자 크기). | [추론] |
| T6 | 상태는 `CartList` 내부 `useState`에 두고 props를 받지 않는다. | [추론] |
| T7 | 목록 마크업은 `ul`/`li`다. | [추론] |
| T8 | 스타일은 새 파일 `src/CartList.css` 하나에 둔다. | [추론] |

### 2-3. 침묵 지점 확정표

| # | 침묵 지점 | 적용한 기본값 | 대안 | 상태 | 번복 조건 |
|---|---|---|---|---|---|
| 1 | 요청 4 «더 이상 담긴 것이 아니다»의 해석 — 줄을 제거하는가, 남기고 표시하는가, 0 도달을 막는가 | 제거 (T1) | 줄을 흐리게 두기 / 수량 1에서 − 를 잠금 | 선택 대기 | 지운 항목을 되살려 달라는 요구가 오면 |
| 2 | 빈 목록 화면 | 문구만, 합계 줄 없음 (T2) | 합계 `0원` 유지 | 선택 대기 | 레이아웃이 흔들린다는 지적이 오면 |
| 3 | 금액 표기 | `1,900원` (T3) | `₩1,900` / 숫자만 | 선택 대기 | 원화 아닌 통화가 들어오면 |
| 4 | 수량 상한 | 없음 (T4) | 99 / 재고 | 선택 대기 | 재고 개념이 도입되면 |
| 5 | 상태 소유자 | 내부 (T6) | props 주입 | 선택 대기 | 부모가 합계를 써야 하면 |
| 6 | 마크업 | `ul`/`li` (T7) | `table` | 선택 대기 | 열이 늘면 |
| 7 | 스타일 위치 | 새 파일 (T8) | `App.css` 확장 | 선택 대기 | 스타일 규약이 제시되면 |

## 3. 완료 전 대조

| # | 지목 위치 | 확인 |
|---|---|---|
| S1 | `src/CartList.tsx:18` | ✅ |
| S2 | `src/App.tsx:1`, `:6` | ✅ |
| S3 | `src/CartList.tsx:43-55` | ✅ |
| S4 | `src/CartList.tsx:47-53` (− / + 두 개, 글리프만) | ✅ |
| S5 | `src/CartList.tsx:25-30` | ✅ |
| S6 | `src/CartList.tsx:31` 필터, 합계 `:34` | ✅ |
| S7 | `src/CartList.tsx:60-62` | ✅ |
| S8 | `src/CartList.tsx:9-12` | ✅ |
| S9 | 실행 확인 — 종료 코드 0 | ✅ |
| T1~T8 | T1 `:31` / T2 `:38-40` / T3 `:14-16` / T4 disabled 없음 / T5 명도 대비 `src/CartList.css:12`, 글자 크기 `:8` / T6 `:19` / T7 `:43` / T8 `src/CartList.css` | ✅ |

**재확인 목록** — §2-3의 7개 행은 모두 `선택 대기`다.
