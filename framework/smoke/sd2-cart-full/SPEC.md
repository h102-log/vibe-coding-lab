# SPEC — CartList (담은 항목 목록 컴포넌트)

작업 문서. 구현 산출물이 아니다. 작성 2026-08-14.

## 0. 스펙 소스

리포에 스펙·계약 문서가 없다 (`README.md`는 Vite 템플릿 안내문이고 요구를 담지 않는다).
따라서 **요청 문장이 유일한 근거**다. 아래 §1은 요청 문장에서 읽어낸 것, §2는 요청이 침묵하는
지점이며 §2 항목은 기본값으로 확정하고 상태를 `선택 대기`로 남긴다.

## 1. 명시된 것

| # | 문장 | 근거 |
|---|---|---|
| S1 | `src/CartList.tsx`가 `CartList` 컴포넌트를 정의해 내보낸다. | 요청 본문 |
| S2 | `src/App.tsx`는 `CartList`를 렌더한다. | 요청 본문 |
| S3 | 담긴 항목이 1개 이상이면 각 항목이 목록의 한 줄로 렌더된다. | 요청 1 |
| S4 | 각 줄은 항목 이름, 단가, 수량, 소계 네 값을 모두 표시하고, 소계 = 수량 × 단가다. | 요청 1 |
| S5 | 각 줄에 감소(−)·증가(+) 버튼 두 개로 된 수량 스테퍼가 있다. | 요청 2 |
| S6 | 두 버튼은 아이콘(기호)만 표시하고 시각적 텍스트 라벨을 갖지 않으며, 작은 크기다. | 요청 2 |
| S7 | + 버튼을 조작하면 그 줄의 수량이 정확히 1 증가한다. | 요청 3 |
| S8 | − 버튼을 조작하면 그 줄의 수량이 정확히 1 감소한다. | 요청 3 |
| S9 | 수량이 0이 된 줄은 DOM에서 사라진다 — `display:none`·`visibility:hidden`·`hidden` 속성이 아니라 언렌더다. | 요청 4 |
| S10 | 목록 아래에 전체 합계가 표시된다. | 요청 5 |
| S11 | 시작 데이터는 `CartList` 내부에 하드코딩된 2~3개 항목이다. | 요청 6 |
| S12 | −·+ 버튼은 스크린 리더가 읽을 접근 가능한 이름을 가진다(감소/증가를 구분하는 이름). | 요청 7 |
| S13 | −·+ 버튼 모두 Tab 키로 도달할 수 있고 키보드(Enter·Space)로 활성화된다. | 요청 7 |
| S14 | 담긴 줄이 0개면 목록 대신 정확히 `담긴 항목이 없습니다` 텍스트가 렌더된다. | 요청 8 |
| S15 | `npm run build`(= `tsc -b` + `vite build`)가 종료 코드 0으로 끝난다. | 완료 조건 |

## 2. 명시되지 않은 것

### 2-1. 점검표

| 범주 | 상태 |
|---|---|
| 1. 기능 범위와 동작 | Partial |
| 2. 도메인·데이터 모델 | Partial |
| 3. 상호작용·UX 흐름 | Partial |
| 3a. 접근성(이름·키보드 도달성) | Partial |
| 4. 비기능 품질 속성 | Missing |
| 5. 통합·외부 의존 | Clear |
| 6. 엣지 케이스·실패 처리 | Partial |
| 7. 제약·트레이드오프 | Partial |
| 8. 용어·일관성 | Partial |
| 9. 완료 신호 | Clear |
| 10. 기타·미정 자리 | Partial |

`Clear` 근거 — 5: 새 의존성 추가 없이 설치된 React 19 + Vite만 쓴다(요청이 "프로젝트는 준비돼
있다"고 못박음). 9: 완료 신호가 `npm run build` 종료 코드 0으로 단일하게 지정됐다.

### 2-2. 추론으로 확정한 문장

| # | 문장 | 근거 |
|---|---|---|
| I1 | 수량이 1인 줄에서 − 를 누르면 수량이 0이 되고 그 줄은 즉시 제거된다 (S8 + S9의 합성). | [추론] |
| I2 | 제거된 줄을 되살리는 수단(되돌리기·다시 담기)은 없다. | [추론] |
| I3 | 수량 상한은 없다 — + 는 항상 활성이다. | [추론] |
| I4 | 금액은 정수 원화이며 `1,200원`처럼 천 단위 구분 기호와 `원` 접미사로 표기한다. | [추론] |
| I5 | 전체 합계 = 현재 렌더 중인 모든 줄의 소계 합. 제거된 줄은 합계에 포함되지 않는다. | [추론] |
| I6 | 목록이 비면 합계 줄도 함께 렌더되지 않는다 — 빈 상태에는 `담긴 항목이 없습니다`만 남는다. | [추론] |
| I7 | 목록 마크업은 `ul`/`li`이고, 각 값은 단위·라벨을 포함한 텍스트(`단가 1,200원`, `2개`, `소계 2,400원`)로 읽힌다. | [추론] |
| I8 | 버튼의 접근 가능한 이름에는 항목 이름이 포함된다(`사과 수량 증가`) — 한 화면에 같은 이름의 버튼이 여러 개 있으면 스크린 리더 사용자가 줄을 구분할 수 없기 때문. | [추론] |
| I9 | − 로 줄이 제거되면 포커스는 같은 자리의 다음 줄 − 버튼으로, 없으면 이전 줄 − 버튼으로, 목록이 비면 빈 상태 텍스트로 이동한다. | [추론] |
| I10 | 수량 변화를 `aria-live`로 별도 방송하지 않는다 — 버튼 이름과 수량 텍스트로 충분하다고 본다. | [추론] |
| I11 | 각 항목은 이름과 별개인 안정적 `id`로 식별되며 React `key`로 쓴다(이름 중복에 견디게). | [추론] |
| I12 | `CartList`는 props를 받지 않는다 — 상태와 초기 데이터를 컴포넌트가 소유한다(S11의 귀결). | [추론] |
| I13 | 스타일은 `src/CartList.css` 한 파일에 두고 `CartList.tsx`가 import한다. 기존 `App.css`·`index.css`는 수정하지 않는다. | [추론] |
| I14 | 스테퍼 버튼은 시각적으로 작되 최소 터치 타깃 32×32px를 지킨다("작은 버튼" 요구와 조작성의 절충). | [추론] |
| I15 | 동작 검증용 vitest 테스트를 `src/CartList.test.tsx`에 둔다. `tsconfig.app.json`의 `exclude`가 `*.test.*`를 제외하므로 `npm run build` 결과에 영향을 주지 않는다. | [추론] |
| I16 | `package.json`·`vite.config.ts`·`tsconfig*.json`은 수정하지 않는다. 테스트는 `npx vitest run`으로 실행하고 jsdom 환경은 테스트 파일 상단 주석으로 지정한다. | [추론] |
| I17 | 반응형·다크 모드·애니메이션·영속화(localStorage)·서버 동기화는 범위 밖이다. | [추론] |

`[MISSING: 성능 목표]` — 항목 수 상한과 렌더 성능 기준을 정할 근거가 없다. 하드코딩 2~3개
기준이므로 최적화(memo·가상화) 없이 진행한다.
`[MISSING: 디자인 시안]` — 색·간격·타이포의 근거가 없다. `index.css`의 CSS 변수를 재사용한다.

### 2-3. 침묵 지점 확정표

| # | 침묵 지점 | 적용한 기본값 | 대안 | 상태 | 번복 조건 |
|---|---|---|---|---|---|
| 1 | 수량 1에서 − 를 누르면? | 0으로 내려가며 줄 제거 (I1) | 1에서 − 를 `disabled` 처리해 제거를 막음 / 확인 대화상자 | 선택 대기 | 실수로 항목이 사라진다는 불만이 나오면 |
| 2 | 제거 되돌리기 | 없음 (I2) | 되돌리기 토스트 / 삭제됨 영역 | 선택 대기 | 1번을 그대로 두는데 오조작이 잦으면 |
| 3 | 수량 상한 | 없음 (I3) | 99 상한 / 재고 수량 상한 | 선택 대기 | 재고·주문 한도 개념이 도입되면 |
| 4 | 금액 표기 | `1,200원` (I4) | `₩1,200` / 숫자만 / 소수점 통화 | 선택 대기 | 원화 아닌 통화나 소수점 단가가 들어오면 |
| 5 | 빈 목록일 때 합계 | 합계 줄도 렌더 안 함 (I6) | `전체 합계 0원`을 계속 표시 | 선택 대기 | 합계 자리가 사라져 레이아웃이 흔들린다는 지적이 나오면 |
| 6 | 목록 마크업 시맨틱 | `ul`/`li` + 값마다 단위 텍스트 (I7) | `table`(열 헤더로 이름·단가·수량·소계) / `div` | 선택 대기 | 열이 늘어 표 성격이 강해지면 |
| 7 | 버튼 접근 가능한 이름 | 항목명 포함 — `사과 수량 증가` (I8) | 요청 예시 그대로 `수량 증가` / `aria-labelledby`로 줄 이름 참조 | 선택 대기 | 요청 예시 문구를 그대로 고정해야 하면 |
| 8 | 줄 제거 시 포커스 | 이웃 − 버튼 → 없으면 빈 상태 텍스트 (I9) | 아무 처리 안 함(포커스가 body로 이탈) / 목록 컨테이너로 이동 | 선택 대기 | 포커스 이동이 되레 혼란스럽다는 피드백이 나오면 |
| 9 | 수량 변경 방송 | `aria-live` 없음 (I10) | 소계·합계 영역에 `aria-live="polite"` | 선택 대기 | 스크린 리더 사용자가 변경을 놓친다는 보고가 나오면 |
| 10 | 수량 직접 입력 | 없음 — 스테퍼로만 조작 (요청 2·3의 범위) | 숫자 입력 필드 병기 | 선택 대기 | 수량이 큰 항목을 다루게 되면 |
| 11 | 테스트 산출 여부 | 검증용 테스트 작성, 빌드 파이프라인 무수정 (I15·I16) | 테스트 없음 / `npm test` 스크립트 추가 | 선택 대기 | 테스트를 CI에서 돌려야 하면 |
| 12 | 스타일 범위 | 최소 CSS 새 파일 (I13·I14) | 스타일 없음(시맨틱만) / 기존 `App.css` 확장 | 선택 대기 | 디자인 시스템이 지정되면 |

## 3. 완료 전 대조

구현 완료 선언 전에 아래 표를 채운다 — 각 문장을 참으로 만드는 코드의 파일·줄을 지목한다.

| 문장 | 구현 위치 | 확인 |
|---|---|---|
| S1 | `src/CartList.tsx:29` (`export default function CartList`) | ✅ |
| S2 | `src/App.tsx:1`(import) + `src/App.tsx:7`(`<CartList />`) | ✅ |
| S3 | `src/CartList.tsx:93-94` (`items.map` → `<li key>`) | ✅ |
| S4 | 이름 `:95`, 단가 `:96`, 수량 `:112`, 소계 `:122-124`, 계산 `:25-27` (`subtotalOf`) | ✅ |
| S5 | `src/CartList.tsx:97-121` (`.cart-stepper` 안 − 버튼 `:98-111`, + 버튼 `:113-120`) | ✅ |
| S6 | `src/CartList.tsx:110,119` (버튼 자식이 `−`/`+` 기호뿐이며 `aria-hidden`) + `src/CartList.css:50-62` | ✅ |
| S7 | `src/CartList.tsx:47-53` (`handleIncrease`, `quantity + 1`) — 테스트 `CartList.test.tsx:41` | ✅ |
| S8 | `src/CartList.tsx:59-65` (`handleDecrease`, `quantity - 1`) — 테스트 `CartList.test.tsx:54` | ✅ |
| S9 | `src/CartList.tsx:69-70` (`items.filter`로 배열에서 제외 → 언렌더) — 테스트 `CartList.test.tsx:65` | ✅ |
| S10 | `src/CartList.tsx:128-130` (합계 줄), 계산 `:88` | ✅ |
| S11 | `src/CartList.tsx:14-18` (`INITIAL_ITEMS` 3개) | ✅ |
| S12 | `src/CartList.tsx:101,116` (`aria-label`) | ✅ |
| S13 | `src/CartList.tsx:98-99,113-114` (네이티브 `<button type="button">` — 기본 탭 순서·Enter/Space 활성화) — 테스트 `CartList.test.tsx:89` | ✅ |
| S14 | `src/CartList.tsx:78-86` (빈 배열이면 이른 반환, 문구 `:82`) | ✅ |
| S15 | `npm run build` 종료 코드 0 (2026-08-14 실행 확인) | ✅ |
| I1 | `src/CartList.tsx:59,66-70` (`quantity > 1`이 아니면 제거 경로) | ✅ |
| I2 | 되살리기 UI 없음 — 부재로 충족 | ✅ |
| I3 | `src/CartList.tsx:113-120` (+ 버튼에 `disabled` 없음) | ✅ |
| I4 | `src/CartList.tsx:20-23` (`formatWon`) | ✅ |
| I5 | `src/CartList.tsx:88` (`items.reduce`, 렌더 중인 `items`만 합산) | ✅ |
| I6 | `src/CartList.tsx:78-86` (빈 상태 이른 반환이 합계 렌더보다 앞) | ✅ |
| I7 | `src/CartList.tsx:92-127` (`ul`/`li` + `단가 …`·`…개`·`소계 …` 단위 텍스트) | ✅ |
| I8 | `src/CartList.tsx:101,116` (`${item.name} 수량 감소`/`증가`) | ✅ |
| I9 | `src/CartList.tsx:37-45`(포커스 적용) + `:72-75`(대상 선정) + `:81`(빈 상태 `tabIndex={-1}`) — 테스트 `CartList.test.tsx:103` | ✅ |
| I10 | `aria-live` 없음 — 부재로 충족 | ✅ |
| I11 | `src/CartList.tsx:5,94` (`id` 필드 + `key={item.id}`) | ✅ |
| I12 | `src/CartList.tsx:29` (매개변수 없음) | ✅ |
| I13 | `src/CartList.css` 신규, `src/CartList.tsx:2` import. `App.css`·`index.css` 무수정 | ✅ |
| I14 | `src/CartList.css:51-52` (`min-width`/`min-height: 32px`) | ✅ |
| I15 | `src/CartList.test.tsx` 8케이스 전부 통과(`npx vitest run`), `tsconfig.app.json:27` exclude로 빌드 제외 | ✅ |
| I16 | `package.json`·`vite.config.ts`·`tsconfig*.json` 변경 없음 (`git status`로 확인) | ✅ |
| I17 | 해당 기능 없음 — 부재로 충족 | ✅ |

**재확인 요청 목록** — §2-3의 12개 행은 모두 `선택 대기`다. 특히 5번(빈 목록 합계), 7번(버튼
접근 이름에 항목명 포함), 8번(제거 시 포커스 이동)은 답에 따라 구현이 갈리는 지점이라 사용자
확정이 필요하다.
