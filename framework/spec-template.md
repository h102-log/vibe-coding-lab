# SPEC — <대상 이름>

<!--
  이 파일은 framework/spec-template.md 의 사본이다.

  ⚠ 이 골격은 «정답»이 아니라 **위임 시 쓰는 기본값**이다.
     프로젝트의 첫 SPEC을 쓰기 전에 골격을 사용자에게 제시하고 확정 절차(§0)를 거친다.
     한 번 확정되면 그 프로젝트 안에서는 고정이고, 이후 SPEC과 검사기가 그것을 따른다.

  확정된 뒤에는 헤딩 텍스트·표 헤더·상태 리터럴이 검사 대상이므로 고치지 않는다. 내용만 채운다.
  문장 ID 접두어 3종이 규약이다 — S(§1 명시) · I(§2.2 추론) · U(§2.3 미확정).
  채우고 나면 이 주석 블록은 지운다.
-->

## 0. 스펙 소스 판별

<!-- 스펙·계약 문서가 있으면 그 경로. 없으면 «요청 문장이 근거»라고 적는다. -->

- 스펙 소스: <경로 또는 «요청 문장»>
- 골격: <출처> · 확정 방식 **직접 정의 / 선택 / 위임** 중 하나 · 상태 `확정` 또는 `선택 대기`

<!-- 골격 확정도 §2의 확정 절차와 같은 3방식이다.
       직접 정의 — 사용자가 골격을 직접 쓰거나 고쳤다 (근거 = 사용자, 날짜)
       선택     — 골격 안 2~4개를 권장 표시와 함께 제시하고 사용자가 골랐다
       위임     — 물었으나 답을 받지 못했다. `framework/spec-template.md`로 진행하고 상태는 `선택 대기`.
                  이 경우 §2.3에 골격 행을 하나 남긴다 — 나중에 뒤집을 수 있어야 하기 때문이다.
     물어보지 않고 진행한 경우는 셋 중 어느 것도 아니다. 그건 기록 없는 확정이다. -->

## 1. 명시된 것

<!-- 계약/요청에서 읽어낸 검증 가능한 문장. 원문 복사 금지, 문장 끝에 근거 위치를 단다.
     확정된 것만 여기 쓴다 — `선택 대기` 항목은 §2.3에만 두고, 확정된 순간에만 올린다. -->

- S1. <조건>일 때 <주체>는 <관측 가능한 동작>을 한다. (근거: <위치>)
- S2.

## 2. 명시되지 않은 것

### 2.1 점검표

<!-- 10범주 전부를 한 행씩 남긴다. 행을 지우지 않는다.
     상태는 Clear / Partial / Missing 셋 중 하나(리터럴). -->

| 범주 | 상태 |
| --- | --- |
| 1. 기능 범위와 동작 (Functional Scope & Behavior) | |
| 2. 도메인·데이터 모델 (Domain & Data Model) | |
| 3. 상호작용·UX 흐름 (Interaction & UX Flow) — UI 조작 요소가 있으면 접근 가능한 이름·키보드 도달성 포함 | |
| 4. 비기능 품질 속성 (Non-Functional Quality Attributes) | |
| 5. 통합·외부 의존 (Integration & External Dependencies) | |
| 6. 엣지 케이스·실패 처리 (Edge Cases & Failure Handling) | |
| 7. 제약·트레이드오프 (Constraints & Tradeoffs) | |
| 8. 용어·일관성 (Terminology & Consistency) | |
| 9. 완료 신호 (Completion Signals) | |
| 10. 기타·미정 자리 (Misc / Placeholders) | |

### 2.2 추론으로 확정한 문장

<!-- §1과 같은 형식의 검증 가능한 문장. 근거 위치 대신 `[추론]`을 단다.
     근거로 정할 수 없으면 문장 대신 `[MISSING: <주제>]`로 남긴다. -->

- I1. <문장> `[추론]`
- I2.

### 2.3 미확정 항목 (기본값으로 진행, 확정은 사용자 몫)

<!-- 6열 고정. 사용자가 확정하기 전까지 상태는 `선택 대기` 리터럴이다.
     ⚠ 이 표의 기본값은 «아직 안 정해진 것»이다 — 테스트 단언으로 고정하지 않는다.
        테스트가 필요하면 그 행을 먼저 확정해 §1으로 올린 뒤에 쓴다. -->

| # | 침묵 지점 | 적용한 기본값 | 대안 | 상태 | 번복 조건 |
| --- | --- | --- | --- | --- | --- |
| U1 | | | | 선택 대기 | |
| U2 | | | | 선택 대기 | |

## 3. 완료 전 대조

<!-- 완료를 선언하기 **전에** 채운다. "있을 것이다"가 아니라 파일·줄을 지목한다.
     지목하지 못하면 그 문장은 구현되지 않은 것이다 — 구현하거나 기각 사유를 적는다. -->

### 3.1 문장 대조

<!-- §1의 S 전건 + §2.2의 I 전건이 빠짐없이 여기 나와야 한다. -->

| 문장 | 코드 위치 |
| --- | --- |
| S1 | |
| I1 | |

### 3.2 재확인 목록 (`선택 대기`)

<!-- §2.3에서 상태가 `선택 대기`인 행 전건. 사용자에게 함께 제시한다. -->

- U1. <침묵 지점> — 기본값 <무엇>으로 진행 중. 확정 필요.
